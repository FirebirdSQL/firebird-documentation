<?xml version="1.0" encoding="UTF-8"?>
<!-- For standalone validation, uncomment the following DOCTYPE declaration. -->
<!--
<!DOCTYPE article PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
"../../../tools/docbook-dtd/docbookx.dtd">
-->
<article id="qsg25">
  <articleinfo>
    <title>Firebird 2.5 Quick Start Guide</title>
    <titleabbrev>Firebird 2.5 Quick Start</titleabbrev>
    <authorgroup>
      <corpauthor>IBPhoenix Editors</corpauthor>
      <corpauthor>Firebird Project members</corpauthor>
    </authorgroup>
    <edition>8 July 2011, document version 4.3 — covers Firebird 2.5</edition>
  </articleinfo>
  <!--Trusted login from a remote client is no longer possible from 2.0 onward. 2.0 Notes: "Login with password is required from any remote client."
Notice that Windows trusted login (introduced in 2.1) *is* possible from a remote client, but within the same domain and with his credentials
verified bij the Windows Domain Whatchamacallit.-->
  <section id="qsg25-about">
    <title>About this guide</title>
    <para>The <citetitle>Firebird Quick Start Guide</citetitle> is an introduction for the complete
    newcomer to a few essentials for getting off to a quick start with a Firebird binary kit. The
    guide first saw the light as Chapter 1 of the <citetitle>Using Firebird</citetitle> manual, sold
    on CD by <ulink url="http://www.ibphoenix.com">IBPhoenix</ulink>. Later it was published
    separately on the Internet. In June 2004, IBPhoenix donated it to the Firebird Project. Since
    then it is maintained, and regularly updated, by members of the Firebird documentation
    project.</para>
    <important>
      <para>Before you read on, verify that this guide matches your Firebird version. This guide
      covers version 2.5. For all other Firebird versions, get the corresponding Quick Start Guide
      at <ulink
      url="http://www.firebirdsql.org/en/documentation/">http://www.firebirdsql.org/en/documentation/</ulink>.</para>
    </important>
  </section>
  <section id="qsg25-firebird-licenses">
    <title>The Firebird licenses<indexterm>
        <primary>Licenses</primary>
      </indexterm><indexterm>
        <primary>Firebird licenses</primary>
      </indexterm><indexterm>
        <primary>IPL</primary>
      </indexterm><indexterm>
        <primary>IDPL</primary>
      </indexterm></title>
    <para>Firebird is a free, open-source database management system, but <quote>free</quote> does
    not mean that everything is permitted. The use of Firebird is governed by two licenses: the IPL
    (InterBase Public License) and the IDPL (Initial Developer's Public License). The first one
    covers the parts of the source code that were inherited from InterBase; the second applies to
    the additions and improvements made by the Firebird Project. Both licenses offer similar rights
    and restrictions. In short:</para>
    <itemizedlist>
      <listitem>
        <para>Use of the software is free, even for commercial purposes. You may also redistribute
        the software, separately or with a product of your own, but you may not claim ownership or
        credit for it. Any license notices included with Firebird must remain intact.</para>
      </listitem>
      <listitem>
        <para>You may modify and recompile the Firebird source code or parts of it. You may
        distribute such modified versions, but if you do so, you <emphasis>must</emphasis> document
        your modifications and make them publicly available, at no cost, under the same license as
        the original code.</para>
      </listitem>
      <listitem>
        <para>You may include Firebird source code (modified or not) in a larger work and distribute
        that larger work, in source and/or compiled form, under a license of your own choosing. You
        need not publicize the source code for the entire larger work, but you
        <emphasis>must</emphasis> fulfill the license conditions for the parts that were taken from
        Firebird, whether they were modified or not.</para>
      </listitem>
    </itemizedlist>
    <para>Please notice that the above is a simplified overview. Only the original license texts are
    legally binding. You can find them here:</para>
    <blockquote>
      <para><ulink url="http://www.firebirdsql.org/ipl/">http://www.firebirdsql.org/ipl/</ulink>
      (IPL)</para>
      <para><ulink url="http://www.firebirdsql.org/idpl/">http://www.firebirdsql.org/idpl/</ulink>
      (IDPL)</para>
    </blockquote>
  </section>
  <section id="qsg25-classic-or-super">
    <title><indexterm class="singular" scope="local">
        <primary>Installation</primary>
        <secondary>Classic or Superserver</secondary>
      </indexterm><indexterm class="singular" scope="local">
        <primary>Classic Server</primary>
      </indexterm><indexterm class="singular" scope="local">
        <primary>Superserver</primary>
      </indexterm><indexterm class="singular" scope="local">
        <primary>SuperClassic</primary>
      </indexterm><indexterm>
        <primary>Architectures</primary>
      </indexterm><indexterm>
        <primary>Server architectures</primary>
      </indexterm>Classic, SuperClassic or Superserver?</title>
    <para>Firebird servers come in two flavours, called <firstterm>architectures</firstterm>:
    Classic Server and Superserver. Since Firebid 2.5, Classic Server can operate in two modes:
    <quote>traditional</quote> Classic and SuperClassic, giving a total of 3 models. Which one
    should you choose? The most important differences are listed below. In the vast majority of
    cases, all three models perform equally well and offer (almost) the same possibilities.</para>
    <variablelist>
      <varlistentry>
        <term>Processes</term>
        <listitem>
          <para>Classic uses a separate process for each connection; SuperClassic and Superserver
          use a single process. Thus, if a Classic server process crashes, the other connections
          remain unaffected. With SuperClassic and Superserver, a crash take down all the
          connections.</para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Guardian</term>
        <listitem>
          <para>Superserver can run under the control of the Firebird Guardian, which automatically
          restarts it in case of a crash. SuperClassic only offers the Guardian option under Linux.
          Classic doesn't offer it at all.</para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Resources</term>
        <listitem>
          <para>Being single-process, SuperClassic and Superserver use system resources more
          efficiently than Classic if the number of simultaneous connections grows. Superserver is
          the most efficient of the three, because it also has a shared cache space.</para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Local connections</term>
        <listitem>
          <para>Classic and SuperClassic offer an <quote>embedded</quote> local connection mode on
          Linux which is very fast, but not as secure as a regular network connection. On Windows, a
          separate <link linkend="qsg25-classic-or-super-embedded">Embedded Server</link> is
          available which is even less secure, but can be very practical if you want to ship
          Firebird with your applications.</para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Simultaneous connections</term>
        <listitem>
          <para>Only Classic and SuperClassic allow simultaneous connections to a database from the
          regular server and one or more embedded servers. Thus, if you use the Windows Embedded
          Server, it may be advantageous to have Classic or SuperClassic as your regular
          server.</para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>Multiprocessing</term>
        <listitem>
          <para>On Windows, Superserver defaults to using only the first processor or core in your
          computer. To make it use all the available CPU power, the
          <parameter>CpuAffinityMask</parameter> parameter in <filename>firebird.conf</filename>
          must be edited. All other servers (including Superserver for Linux) support
          multiprocessing out of the box and ignore <parameter>CpuAffinityMask</parameter>.</para>
        </listitem>
      </varlistentry>
    </variablelist>
    <para>As you can see, none of the three models outshines the other two on all fronts. If you're
    not sure which is best for you, SuperClassic may be a good pick on 64-bit systems. Make sure you
    install the 64-bits version, though. On 32-bits systems, SuperClassic will be the first to run
    out of memory under high load. Superserver, with its shared cache space, and Classic, with its
    separate processes, perform better there.</para>
    <para>Notice that you can always switch to another model later; your applications and databases
    will keep functioning like before. The differences are in the servers, not in the
    databases.</para>
    <para>For a more detailed look at the various server models, consult the appendix <link
    linkend="qsg25-appx-architectures">Firebird server architectures</link>.</para>
    <section id="qsg25-classic-or-super-packages">
      <title>Installation packages</title>
      <para>For Linux, Superserver download packages start with <literal>FirebirdSS</literal>,
      Classic/SuperClassic packages with <literal>FirebirdCS</literal>. The default run mode for
      Classic/SuperClassic installations is traditional, multiprocess Classic. To switch to
      SuperClassic, run the script <filename>changeMultiConnectMode.sh</filename> and answer
      <quote><userinput>thread</userinput></quote> at the prompt (for
      multi<emphasis>threaded</emphasis>). The script is located in the <filename
      class="directory">bin</filename> subdirectory of your Firebird installation.</para>
      <para>For Windows, there is a combined installation package; you choose the architecture
      (Superserver or Classic) on one of the first screens. If you choose Classic, you can enable
      SuperClassic mode a couple of screens later.</para>
    </section>
    <section id="qsg25-classic-or-super-embedded">
      <title>Embedded Server for Windows<indexterm>
          <primary>Embedded Server</primary>
        </indexterm><indexterm>
          <primary>Windows Embedded Server</primary>
        </indexterm></title>
      <para>On Windows platforms, the <firstterm>Embedded Server</firstterm> comes in a separate
      download package. It contains a client and server combined into one DLL for ease of
      deployment. Whilst very practical, it lacks most of Firebird's usual security features (much
      more so than Linux's embedded local access). For more information on Windows Embedded Server,
      consult the <citetitle>Clients and Servers</citetitle> chapter in <citetitle>Using
      Firebird</citetitle>:</para>
      <blockquote>
        <para><ulink
        url="http://www.firebirdsql.org/manual/ufb-cs-embedded.html">http://www.firebirdsql.org/manual/ufb-cs-embedded.html</ulink>
        (HTML)</para>
        <para><ulink
        url="http://www.firebirdsql.org/pdfmanual/Using-Firebird_(wip).pdf">http://www.firebirdsql.org/pdfmanual/Using-Firebird_(wip).pdf</ulink>
        (PDF)</para>
        <!--The above links are still valid on the new site.-->
      </blockquote>
      <para><emphasis role="bold">Please notice!</emphasis> At the time of this writing, the
      information at the URLs above is <emphasis role="bold">not yet up to date</emphasis> with the
      situation in Firebird 2.5. The most important change is:</para>
      <itemizedlist>
        <listitem>
          <para>Windows Embedded now contains a SuperClassic instead of a SuperServer engine. File
          locks are shared, so a database can be accessed by one or more Embedded servers and a
          regular Classic or SuperClassic server at the same time.</para>
        </listitem>
      </itemizedlist>
      <para>Consult the Firebird 2.5 Release Notes for full details.</para>
    </section>
  </section>
  <section id="qsg25-kit-contents">
    <title><indexterm class="singular" scope="local">
        <primary>Installation</primary>
        <secondary>kit contents</secondary>
      </indexterm>What is in the kit?</title>
    <para>All of the download kits contain all the components needed to install the Firebird
    server:</para>
    <itemizedlist spacing="compact">
      <listitem>
        <para>The Firebird server executable.</para>
      </listitem>
      <listitem>
        <para>One or more client libraries.</para>
      </listitem>
      <listitem>
        <para>The command-line tools.</para>
      </listitem>
      <listitem>
        <para>The standard user-defined function libraries.</para>
      </listitem>
      <listitem>
        <para>A sample database.</para>
      </listitem>
      <listitem>
        <para>The C header files (not needed by beginners).</para>
      </listitem>
      <listitem>
        <para>Release notes – <emphasis>essential reading!</emphasis></para>
      </listitem>
    </itemizedlist>
  </section>
  <section id="qsg25-disk-locations">
    <title><indexterm>
        <primary>Disk locations</primary>
      </indexterm>Default disk locations</title>
    <section id="qsg25-disk-locations-linux">
      <title><indexterm>
          <primary>Disk locations</primary>
          <secondary>Linux</secondary>
        </indexterm>Linux</title>
      <para>The following table shows the default component locations of a Firebird installation on
      Linux. Some of the locations may be different on other Unix-like systems.</para>
      <table frame="all" id="qsg25-tbl-firebird-components-linux"
             xreflabel="Firebird installation components table">
        <title>Firebird 2.5 component locations on Linux</title>
        <tgroup cols="3">
          <colspec align="left" colname="col-component" colwidth="3*" />
          <colspec align="left" colname="col-filename" colwidth="3*" />
          <colspec align="left" colname="col-location" colwidth="3*" />
          <thead>
            <row>
              <entry><para>Component</para></entry>
              <entry><para>File Name</para></entry>
              <entry><para>Default Location</para></entry>
            </row>
          </thead>
          <tbody valign="top">
            <row>
              <entry><para>Installation directory</para><para>(referred to hereafter as
              <varname>&lt;InstallDir&gt;</varname>)</para></entry>
              <entry><para>—</para></entry>
              <entry><para><filename class="directory">/opt/firebird</filename></para><para>(may
              vary per distribution)</para></entry>
            </row>
            <row>
              <entry><para>Configuration files</para></entry>
              <entry><para><filename>firebird.conf</filename>, <filename>aliases.conf</filename>,
              etc.</para></entry>
              <entry><para><filename class="directory">&lt;InstallDir&gt;</filename></para></entry>
            </row>
            <row>
              <entry><para>Release Notes and other documentation</para></entry>
              <entry><para>Various files</para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;/doc</filename></para></entry>
            </row>
            <row>
              <entry><para>Firebird server</para></entry>
              <entry><para><filename>fbserver</filename> (SS), <filename>fb_smp_server</filename>
              (SC) or <filename>fb_inet_server</filename> (CS)</para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;/bin</filename></para></entry>
            </row>
            <row>
              <entry><para>Command-line tools</para></entry>
              <entry><para><filename>isql</filename>, <filename>gbak</filename>,
              <filename>nbackup</filename>, <filename>gsec</filename>, <filename>gfix</filename>,
              <filename>gstat</filename>, etc.</para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;/bin</filename></para></entry>
            </row>
            <row>
              <entry><para>Sample database</para></entry>
              <entry><para><filename>employee.fdb</filename></para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;/examples/empbuild</filename></para></entry>
            </row>
            <row>
              <entry><para>UDF libraries</para></entry>
              <entry><para><filename class="libraryfile">ib_udf.so</filename>, <filename
              class="libraryfile">fbudf.so</filename> </para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;/UDF</filename></para></entry>
            </row>
            <row>
              <entry><para>Additional server-side libraries</para></entry>
              <entry><para><filename class="libraryfile">libicu*.so</filename>, <filename
              class="libraryfile">libib_util.so</filename></para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;/lib</filename></para></entry>
            </row>
            <row>
              <entry><para>Client libraries</para></entry>
              <entry><para><filename
              class="libraryfile">libfbclient.so.2.5.<replaceable>n</replaceable></filename>
              (network client)</para><para><filename
              class="libraryfile">libfbembed.so.2.5.<replaceable>n</replaceable></filename> (local
              client with embedded engine, Classic/SuperClassic only)</para><para>The usual symlinks
              (<filename class="symlink">*.so.2</filename>, <filename
              class="symlink">*.so</filename>) are created. Legacy <filename
              class="symlink">libgds.*</filename> symlinks are also installed.</para></entry>
              <entry><para><filename
              class="directory">/usr/lib[64]</filename></para><para>(actually, the real stuff is in
              <filename class="directory">&lt;InstallDir&gt;/lib</filename>, but you should use the
              links in <filename class="directory">/usr/lib[64]</filename>)</para></entry>
            </row>
          </tbody>
        </tgroup>
      </table>
    </section>
    <section id="qsg25-disk-locations-win">
      <title><indexterm>
          <primary>Disk locations</primary>
          <secondary>Windows</secondary>
        </indexterm>Windows</title>
      <para>In the table below, <filename class="directory">&lt;ProgramDir&gt;</filename> refers to
      the Windows programs folder. This is usually <quote><filename class="directory">C:\Program
      Files</filename></quote> but may also be a different path, e.g. <quote><filename
      class="directory">D:\Programmi</filename></quote>. Likewise, <filename
      class="directory">&lt;SystemDir&gt;</filename> refers to the Windows system directory. Be sure
      to read the notes below the table, especially if you're running Firebird on a 64-bit Windows
      system.</para>
      <table frame="all" id="qsg25-tbl-firebird-components-win"
             xreflabel="Firebird installation components table">
        <title>Firebird 2.5 component locations on Windows</title>
        <tgroup cols="3">
          <colspec align="left" colname="col-component" colwidth="3*" />
          <colspec align="left" colname="col-filename" colwidth="3*" />
          <colspec align="left" colname="col-location" colwidth="3*" />
          <thead>
            <row>
              <entry><para>Component</para></entry>
              <entry><para>File Name</para></entry>
              <entry><para>Default Location</para></entry>
            </row>
          </thead>
          <tbody valign="top">
            <row>
              <entry><para>Installation directory</para><para>(referred to hereafter as
              <varname>&lt;InstallDir&gt;</varname>)</para></entry>
              <entry>—</entry>
              <entry><para><filename
              class="directory">&lt;ProgramDir&gt;\Firebird\Firebird_2_5</filename></para></entry>
            </row>
            <row>
              <entry><para>Configuration files</para></entry>
              <entry><para><filename>firebird.conf</filename>, <filename>aliases.conf</filename>,
              etc.</para></entry>
              <entry><para><filename class="directory">&lt;InstallDir&gt;</filename></para></entry>
            </row>
            <row>
              <entry><para>Release Notes and other documentation</para></entry>
              <entry><para>Various files</para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;\doc</filename></para></entry>
            </row>
            <row>
              <entry><para>Firebird server</para></entry>
              <entry><para><filename>fbserver.exe</filename> (SS) or
              <filename>fb_inet_server.exe</filename> (CS/SC)</para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;\bin</filename></para></entry>
            </row>
            <row>
              <entry><para>Command-line tools</para></entry>
              <entry><para><filename>isql.exe</filename>, <filename>gbak.exe</filename>,
              <filename>nbackup.exe</filename>, <filename>gsec.exe</filename>,
              <filename>gfix.exe</filename>, <filename>gstat.exe</filename>, etc.</para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;\bin</filename></para></entry>
            </row>
            <row>
              <entry><para>Sample database</para></entry>
              <entry><para><filename>employee.fdb</filename></para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;\examples\empbuild</filename></para></entry>
            </row>
            <row>
              <entry><para>User-defined function (UDF) libraries</para></entry>
              <entry><para><filename class="libraryfile">ib_udf.dll</filename>, <filename
              class="libraryfile">fbudf.dll</filename></para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;\UDF</filename></para></entry>
            </row>
            <row>
              <entry><para>Additional server-side libraries</para></entry>
              <entry><para><filename class="libraryfile">icu*.dll</filename>, <filename
              class="libraryfile">ib_util.dll</filename></para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;\bin</filename></para></entry>
            </row>
            <row>
              <entry><para>Client libraries</para></entry>
              <entry><para><filename class="libraryfile">fbclient.dll</filename></para><para>(with
              an optional <filename class="libraryfile">gds32.dll</filename>, to support legacy
              apps)</para></entry>
              <entry><para><filename
              class="directory">&lt;InstallDir&gt;\bin</filename></para><para>(with an optional copy
              in <filename class="directory">&lt;SystemDir&gt;</filename> – see note below
              table)</para></entry>
            </row>
          </tbody>
        </tgroup>
      </table>
      <note>
        <title>The Windows system directory</title>
        <para>The exact path to the Windows System directory depends on your Windows version.
        Typical locations on 32-bit systems are:</para>
        <itemizedlist spacing="compact">
          <listitem>
            <para>for Windows 95/98/ME: <filename
            class="directory">C:\Windows\System</filename></para>
          </listitem>
          <listitem>
            <para>for Windows NT/2000: <filename
            class="directory">C:\WINNT\System32</filename></para>
          </listitem>
          <listitem>
            <para>for Windows XP: <filename class="directory">C:\Windows\System32</filename></para>
          </listitem>
        </itemizedlist>
        <para>For 64-bit systems, read the next note.</para>
      </note>
      <important>
        <title>Important notice for 64-bit Windows users</title>
        <para>On 64-bit Windows systems, the <quote><filename class="directory">Program
        Files</filename></quote> directory is reserved for 64-bit programs. If you try to install a
        32-bit application into that folder, it will be auto-redirected to a directory which – in
        English versions – is called <quote><filename class="directory">Program Files
        (x86)</filename></quote>. In other language versions the name may be different.</para>
        <para>In the same vein, the <filename class="directory">System32</filename> directory is
        reserved for 64-bit libraries. 32-bit libraries go into <filename
        class="directory">SysWOW64</filename>. That's right: 64-bit libraries are in <filename
        class="directory">System32</filename>, 32-bit libraries in <filename
        class="directory">SysWOW64</filename>.</para>
        <para>If you're not aware of this, you may have a hard time locating your 32-bit Firebird
        components on a 64-bit Windows system.</para>
        <para>(Incidentally, <emphasis>WOW</emphasis> stands for <emphasis>Windows on
        Windows</emphasis>. Now you can also work out what <emphasis>LOL</emphasis> means.)</para>
      </important>
    </section>
  </section>
  <section id="qsg25-installing">
    <title><indexterm>
        <primary>Installation</primary>
      </indexterm>Installing Firebird</title>
    <para>The instructions given below for the installation of Firebird on Windows and Linux should
    be sufficient for the vast majority of cases. However, if you experience problems or if you have
    special needs not covered here, be sure to read the <citetitle>INSTALLATION NOTES</citetitle>
    chapter in the Release Notes. This is especially important if you are upgrading from a previous
    version or if there are remnants of an old (and maybe long gone) InterBase or Firebird
    installation floating around your system (DLLs, Registry entries, environment
    variables...)</para>
    <section id="qsg25-installing-server">
      <title><indexterm>
          <primary>Installation</primary>
          <secondary>server</secondary>
        </indexterm>Installing the Firebird server</title>
      <section id="qsg25-installing-server-drives">
        <title><indexterm>
            <primary>Installation</primary>
            <secondary>drives</secondary>
          </indexterm>Installation drives</title>
        <para>Firebird server – and any databases you create or connect to – must reside on a hard
        drive that is physically connected to the host machine. You cannot locate components of the
        server, or any database, on a mapped drive, a filesystem share or a network
        filesystem.</para>
        <note>
          <para>You can mount a read-only database on a CD-ROM drive but you cannot run Firebird
          server from one.</para>
        </note>
      </section>
      <section id="qsg25-installing-server-script">
        <title><indexterm>
            <primary>Installation</primary>
            <secondary>script or program</secondary>
          </indexterm>Installation script or program</title>
        <para>Although it is possible to install Firebird by a filesystem copying method – such as
        <quote>untarring</quote> a snapshot build or decompressing a structured <filename
        class="extension">.zip</filename> archive – it is strongly recommended that you use the
        distributed release kit (<filename>.exe</filename> for Windows, <filename>.rpm</filename>
        for Linux), especially if this is the first time you install Firebird. The Windows
        installation executable, the Linux <application>rpm</application> program and the
        <filename>install.sh</filename> script in the official <filename
        class="extension">.tar.gz</filename> for various Posix platforms all perform some essential
        setup tasks. Provided you follow the installation instructions correctly, there should be
        nothing for you to do upon completion but log in and go!</para>
      </section>
      <section id="qsg25-installing-server-windows">
        <title>Installing on Windows</title>
        <para>The Firebird installer lets you choose between Superserver and Classic/SuperClassic.
        Each model is fully stable and there is no reason to categorically prefer one to the other.
        Of course you may have your own specific considerations. When in doubt, consult the <link
        linkend="qsg25-classic-or-super">Classic, SuperClassic or Superserver</link> chapter
        again.</para>
        <para>If you install Firebird under Windows 95/98/ME, <emphasis
        role="bold">un</emphasis>check the option to install the Control Panel applet. It doesn't
        work on these platforms. You'll find a link to a usable applet further down. (Note: the
        option to install the applet is only available for Superserver.)</para>
        <para>On Windows server platforms – NT, 2000/3/8, XP, Vista and 7 – Firebird will run as a
        system service by default, but during the installation you can also choose to let it run as
        an application. Non-server Windows systems – 95, 98 and ME – don't support services; running
        as an application is the only option there.</para>
        <section id="qsg25-installing-server-windows-guardian">
          <title><indexterm>
              <primary>Guardian</primary>
            </indexterm><indexterm>
              <primary>Firebird Guardian</primary>
            </indexterm>Use the Guardian?</title>
          <para>The Firebird Guardian is a utility that monitors the server process and tries to
          restart it if it terminates abnormally. The Guardian does not work with Firebird Classic
          Server on Windows if run as an application. This is due to a known bug, which may be fixed
          later. Currently the Firebird 2.5 installer doesn't give you the option to include the
          Guardian <emphasis>at all</emphasis> with a Classic Server, even if you install it as a
          service.</para>
          <!--The problem is that the 2.0 Guardian looks for fbserver.exe. In 1.5.3 it's already fixed;
needs to be ported to 2.0. Silly workaround: rename the CS binary to fbserver.exe-->
          <para>The Guardian works correctly with Superserver, whether run as an application or as a
          service.</para>
          <para>If you run Firebird <emphasis>as a service</emphasis> on Windows 2000 or newer, the
          Guardian is a convenience rather than a necessity, since these operating systems have the
          facility to watch and restart services. It is recommended that you keep the Guardian
          option on (if possible) in all other situations.</para>
        </section>
      </section>
      <section id="qsg25-installing-server-linux">
        <title>Installing on Linux and other Unix-like platforms</title>
        <para>In all cases, read the Release Notes that came with your Firebird package (chapter
        <citetitle>Installation Notes</citetitle>, section <citetitle>Posix Platforms</citetitle>).
        There may be significant variations from release to release of any Posix operating system,
        especially the open source ones. Where possible, the build engineers for each Firebird
        version have attempted to document any known issues.</para>
        <para>If you have a Linux distribution that supports <application>rpm</application>
        installs, consult the appropriate platform documentation for instructions about using
        <application>RPM Package Manager</application>. In most distributions you will have the
        choice of performing the install from a command shell or through a GUI interface.</para>
        <para>For Linux distributions that cannot process <application>rpm</application> programs,
        and for Unix flavours for which no <filename class="extension">.rpm</filename> kit is
        provided, use the <filename class="extension">.tar.gz</filename> kit. You will find detailed
        instructions in the Release Notes.</para>
        <para>Shell scripts have been provided. In some cases, the Release Notes may instruct you to
        edit the scripts and make some manual adjustments.</para>
      </section>
    </section>
    <section id="qsg25-installing-multiple">
      <title>Installing multiple servers</title>
      <para>Firebird allows the operation of multiple servers on a single machine. It can also run
      concurrently with Firebird 1.x or InterBase servers. Setting this up is not a beginner's task
      though. If you need to run multiple servers, consult the <citetitle>Installation
      Notes</citetitle> chapter of the Release Notes<!--2.1: Consult the (separate) Installation Guide. 2.5: RlsNotes currently say that 2.1 Installation Guide still applies.-->,
      and have the Firebird 1.5 Release Notes handy too – you will be directed to them at a certain
      point during your reading of the Installation Notes.</para>
    </section>
    <section id="qsg25-installing-testing">
      <title><indexterm>
          <primary>Testing</primary>
        </indexterm>Testing your installation</title>
      <para>If everything works as designed, the Firebird server process will be running on your
      server machine upon completion of the installation. It will also start up automatically
      whenever you restart your computer.</para>
      <para>Before testing the Firebird server itself, it is advisable to verify if the server
      machine is reachable from the client at all. At this point, it is assumed that you will use
      the recommended TCP/IP network protocol for your Firebird client/server connections.</para>
      <note>
        <title>Notes</title>
        <itemizedlist>
          <listitem>
            <para>If you have installed a Classic/SuperClassic Server on Linux/Unix or any Firebird
            server on Windows, it is possible to connect directly to the local server, without using
            a network layer. If you intend to use Firebird for this type of connection <emphasis
            role="bold">only</emphasis>, you can skip the <quote>Pinging the server</quote> section
            below.</para>
          </listitem>
          <listitem>
            <para>For information about using the NetBEUI protocol in an all-Windows environment,
            refer to the <citetitle>Network Configuration</citetitle> chapter in the
            <citetitle>Using Firebird</citetitle> manual sold by IBPhoenix, or consult the InterBase
            6 Operations Guide (<ulink
            url="http://www.ibphoenix.com/files/60OpGuide.zip">http://www.ibphoenix.com/files/60OpGuide.zip</ulink>).</para>
          </listitem>
          <listitem>
            <para>Firebird does not support IPX/SPX networks.</para>
          </listitem>
        </itemizedlist>
      </note>
      <section id="qsg25-installing-testing-ping">
        <title><indexterm>
            <primary>Ping</primary>
          </indexterm>Pinging the server</title>
        <para>The <command>ping</command> command – available on most systems – is a quick and easy
        way to see if you can connect to a server machine via the network. For example, if your
        server's IP address in the domain that is visible to your client is <systemitem
        class="ipaddress">192.13.14.1</systemitem>, go to a command shell on the client machine and
        type the command</para>
        <blockquote>
          <para><command>ping 192.13.14.1</command></para>
        </blockquote>
        <para>substituting this example IP address with the IP address that your server is
        broadcasting. If you are on a managed network and you don't know the server's IP address,
        ask your system administrator. Of course you can also ping the server by its name, if you
        know it:</para>
        <blockquote>
          <para><command>ping vercingetorix</command></para>
        </blockquote>
        <para>If you are connecting to the server from a local client – that is, a client running on
        the same machine as the server – you can ping the virtual TCP/IP loopback server:</para>
        <blockquote>
          <para><command>ping localhost</command> –or– <command>ping 127.0.0.1</command></para>
        </blockquote>
        <para>If you have a simple network of two machines linked by a crossover cable, you can set
        up your server with any IP address you like except 127.0.0.1 (which is reserved for a local
        loopback server) and, of course, the IP address which you are using for your client machine.
        If you know the <quote>native</quote> IP addresses of your network cards, and they are
        different, you can simply use those.</para>
        <para>Once you have verified that the server machine is reachable from the client, you can
        go on to the next step.</para>
      </section>
      <section id="qsg25-installing-testing-check">
        <title><indexterm>
            <primary>Checking the server</primary>
          </indexterm>Checking that the Firebird server is running</title>
        <para>After installation, Firebird server should be running:</para>
        <variablelist>
          <varlistentry>
            <term>On Linux or other Unix-like systems:</term>
            <listitem>
              <para>As a service.</para>
            </listitem>
          </varlistentry>
          <varlistentry>
            <term>On Windows server systems (NT, 2000/3/8, XP, Vista, 7):</term>
            <listitem>
              <para>As a service or as an application. Service is default and highly
              recommended.</para>
            </listitem>
          </varlistentry>
          <varlistentry>
            <term>On Windows non-server systems (95, 98, ME):</term>
            <listitem>
              <para>As an application.</para>
            </listitem>
          </varlistentry>
        </variablelist>
        <para>The following sections show you how to test the server in each of these
        situations.</para>
        <section id="qsg25-installing-testing-check-linux">
          <title>Server check: Linux and other Unices</title>
          <para><indexterm>
              <primary>top command (Linux)</primary>
            </indexterm><indexterm>
              <primary>Guardian</primary>
            </indexterm>Use the <command>top</command> command in a command shell to inspect the
          running processes interactively. If a Firebird Superserver is running, you should see a
          process named <systemitem class="process">fbguard</systemitem>. This is the Guardian
          process. Further, there will be one main and zero or more child processes named
          <systemitem class="process">fbserver</systemitem>.</para>
          <para>The following screen shows the output of <application>top</application>, restricted
          by <application>grep</application> to show only lines containing the characters
          <literal>fb</literal>:</para>
          <screen>frodo:/inkomend/firebird # top -b -n1 | grep fb
 2587 firebird  24   0  1232 1232 1028 S  0.0  0.3   0:00.00 fbguard
 2588 firebird  15   0  4124 4120 2092 S  0.0  0.9   0:00.04 fbserver
 2589 firebird  15   0  4124 4120 2092 S  0.0  0.9   0:00.00 fbserver
 2604 firebird  15   0  4124 4120 2092 S  0.0  0.9   0:00.00 fbserver
 2605 firebird  15   0  4124 4120 2092 S  0.0  0.9   0:00.02 fbserver
 2606 firebird  15   0  4124 4120 2092 S  0.0  0.9   0:00.00 fbserver
 2607 firebird  15   0  4124 4120 2092 S  0.0  0.9   0:00.00 fbserver</screen>
          <para>As an alternative to <application>top</application>, you can use <command>ps
          -ax</command> or <command>ps -aux</command> and pipe the output to
          <application>grep</application>.</para>
          <para>For Classic Server, the process name is <systemitem
          class="process">fb_inet_server</systemitem>. There will be one instance of this process
          running for each network connection. Note that if there are no active connections, or if
          there are only direct local connections, you won't find <systemitem
          class="process">fb_inet_server</systemitem> in the process list. <systemitem
          class="process">fb_lock_mgr</systemitem> should be present though as soon as any kind of
          Classic connection has been established.</para>
          <para>For SuperClassic, the process name is <systemitem
          class="process">fb_smp_server</systemitem> and it will visible as soon as the service is
          started. SuperClassic, just like Superserver, does its own port-listening, so it will be
          running even if there are no connections.</para>
          <para>Other ways to test a Firebird server immediately after installation include
          connecting to a database, creating a database, and launching the
          <application>gsec</application> utility. All these operations are described later on in
          this guide.</para>
        </section>
        <section id="qsg25-installing-testing-check-win-service">
          <title>Server check: Windows, running as service</title>
          <para>Open Control Panel -&gt; Services (NT) or Control Panel -&gt; Administrative Tools
          -&gt; Services (2000/3/8, XP, Vista, 7).</para>
          <para><indexterm>
              <primary>Services (Windows)</primary>
            </indexterm>This illustration shows the Services applet display on Windows 2000. The
          appearance may vary from one Windows server edition to another. Also, service names may
          vary with the Firebird version.</para>
          <mediaobject>
            <imageobject>
              <imagedata align="center" fileref="images/services-2.png" format="PNG" />
            </imageobject>
          </mediaobject>
          <para>You should at least find the Firebird server in the services listing. The Guardian
          may or may not be running, depending on the choices you made during installation.</para>
        </section>
        <section id="qsg25-installing-testing-check-win-app">
          <title>Server check: Windows, running as application</title>
          <para>If Firebird is up and running as an application, it is represented by an icon in the
          system tray:</para>
          <itemizedlist spacing="compact">
            <listitem>
              <para>A green and grey server symbol if controlled by the Guardian;</para>
            </listitem>
            <listitem>
              <para>A round yellow and black graphic if running standalone.</para>
            </listitem>
          </itemizedlist>
          <para>A flashing icon indicates that the server is in the process of starting up (or at
          least trying to do so). A red icon, or an icon with an overlying red stop sign, indicates
          that startup has failed.</para>
          <para>One way to make 100% sure if the server is running or not is to press Ctrl-Alt-Del
          and look for the <systemitem class="process">fbserver</systemitem> or <systemitem
          class="process">fb_inet_server</systemitem> process (and possibly <systemitem
          class="process">fbguard</systemitem>) in the task list.</para>
          <para>On some occasions, you may need to start the Guardian or server once explicitly via
          the Start menu even if you opted for <quote>Start Firebird now</quote> at the end of the
          installation process. Sometimes a reboot is necessary.</para>
          <para>If you're desperately trying to start Firebird and nothing seems to work, ask
          yourself if you've installed Firebird 2.5 (Super)Classic server with the Guardian option
          enabled (the installation program doesn't offer this possibility anymore, but there are
          other ways). As said before, the combination (Super)Classic + Guardian currently doesn't
          work if Firebird runs as an application. Uninstall Firebird if necessary and reinstall
          (Super)Classic <emphasis>without</emphasis> Guardian, or Superserver with or without
          Guardian.</para>
          <para>You can shut the server down via the menu that appears if you right-click on the
          tray icon. Notice that this also makes the icon disappear; you can restart Firebird via
          the Start menu.</para>
          <note>
            <para>Windows Classic Server (but not SuperClassic!) launches a new process for every
            connection, so the number of <systemitem class="process">fb_inet_server</systemitem>
            processes will always equal the number of client connections plus one. Shutdown via the
            tray icon menu only terminates the first process (the <firstterm>listener</firstterm>).
            Other processes, if present, will continue to function normally, each terminating when
            the client disconnects from the database. Of course, once the listener has been shut
            down, new connections can't be made.</para>
          </note>
          <para>In the case of Superserver you can also use a Control Panel applet to check and
          alter the Firebird server status. Some available applets will be presented a little later
          in this guide.</para>
        </section>
      </section>
    </section>
    <section id="qsg25-installing-client">
      <title><indexterm class="singular" scope="local">
          <primary>Installation</primary>
          <secondary>client-only</secondary>
        </indexterm>Performing a client-only install</title>
      <para>Each remote client machine needs to have the client library – <filename
      class="libraryfile">libfbclient.so</filename> on Posix clients, <filename
      class="libraryfile">fbclient.dll</filename> on Windows clients – that matches the release
      version of the Firebird server.</para>
      <para>Firebird versions from 1.5 onward can install symlinks or copies named after the 1.0
      libs (with the <quote>old</quote> InterBase names), to maintain compatibility with third-party
      products which need these files.</para>
      <para>Some extra pieces are also needed for the client-only install.</para>
      <section id="qsg25-installing-client-windows">
        <title>Windows</title>
        <para>At present, no separate installation program is available to install only the client
        pieces on a Windows machine. If you are in the common situation of running Windows clients
        to a Linux or other Unix-like Firebird server (or another Windows machine), you need to
        download the full Windows installation kit that corresponds to the version of Firebird
        server you install on your server machine.</para>
        <para>Fortunately, once you have the kit, the Windows client-only install is easy to do.
        Just run the installation program, and when you arrive at at the <quote>Select
        Components</quote> screen, choose one of the client-only options from the drop-down list or
        uncheck the <quote>Server Components</quote> checkbox.</para>
      </section>
      <section id="qsg25-installing-client-linux">
        <title>Linux and some other Posix clients</title>
        <para>A small-footprint client install program for Linux clients is not available either.
        Additionally, some Posix flavours – even within the Linux constellation – have somewhat
        idiosyncratic requirements for filesystem locations. For these reasons, not all *x
        distributions for Firebird even contain a client-only install option.</para>
        <para>For most Linux flavours, the following procedure is suggested for a Firebird
        client-only install. Log in as <systemitem class="username">root</systemitem> for
        this.</para>
        <procedure>
          <step>
            <para>Look for <filename
            class="libraryfile">libfbclient.so.2.5.<replaceable>n</replaceable></filename>
            (<replaceable>n</replaceable> being the patch version number) in <filename
            class="directory">/opt/firebird/lib</filename> on the machine where the Firebird server
            is installed. Copy it to <filename class="directory">/usr/lib</filename> on the client
            (or <filename class="directory">/usr/lib64</filename> if both server and client are
            64-bits).</para>
          </step>
          <step>
            <para>Create chained symlinks using the following commands:</para>
            <blockquote>
              <para><command>ln -s /usr/lib/libfbclient.so.2.5.<replaceable>n</replaceable>
              /usr/lib/libfbclient.so.2</command></para>
              <!--Also an so.2.5 in between? No. The symlink scheme for fbclient is:
  so -> so.2 -> so.2.5.0  (skips 2.5)
But for libfbembed:
  so -> so.2.5 -> so.2.5.0  (skips 2)
Ask Dmitry just in case!-->
              <para><command>ln -s /usr/lib/libfbclient.so.2
              /usr/lib/libfbclient.so</command></para>
            </blockquote>
            <para>...replacing <varname><replaceable>2.5.n</replaceable></varname> with your version
            number, e.g. <literal>2.5.0</literal> or <literal>2.5.3</literal></para>
            <para>If you're running applications that expect the legacy libraries to be present,
            also create the following symlinks:</para>
            <blockquote>
              <para><command>ln -s /usr/lib/libfbclient.so /usr/lib/libgds.so.0</command></para>
              <para><command>ln -s /usr/lib/libfbclient.so /usr/lib/libgds.so</command></para>
            </blockquote>
          </step>
          <step>
            <para>Copy <filename>firebird.msg</filename> to the client machine, preferably into the
            <filename class="directory">/opt/firebird</filename> directory. If you place it
            somewhere else, create a system-wide permanent <envar>FIREBIRD</envar> environment
            variable pointing to the right directory, so that the API routines can locate the
            messages.</para>
          </step>
          <step>
            <para>Optionally copy some of the Firebird command-line tools – e.g.
            <application>isql</application> – to the client machine. <emphasis>Note:</emphasis>
            always copy the tools from a Superserver kit, regardless of the architecture of the
            server(s) you're planning to connect to. Tools from Classic distributions terminate
            immediately if they can't find the <filename class="libraryfile">libfbembed</filename>
            library (which is useless for network connections) upon program start.</para>
          </step>
        </procedure>
        <para>Instead of copying the files from a server, you can also pull them out of a Firebird
        <filename class="extension">tar.gz</filename> kit. Everything you need is located in the
        <filename class="directory">/opt/firebird</filename> tree within the
        <filename>buildroot.tar.gz</filename> archive that's packed inside the kit.</para>
      </section>
    </section>
  </section>
  <section id="qsg25-config">
    <title><indexterm>
        <primary>Configuration</primary>
      </indexterm><indexterm>
        <primary>Management</primary>
      </indexterm>Server configuration and management</title>
    <para>There are several things you should be aware of – and take care of – before you start
    using your freshly installed Firebird server. This part of the manual introduces you to some
    useful tools and shows you how to protect your server and databases.</para>
    <section id="qsg25-config-gsec">
      <title><indexterm>
          <primary>gsec</primary>
        </indexterm>User management: gsec</title>
      <para>Firebird comes with a command-line user management tool called
      <firstterm>gsec</firstterm>. Although its functions can also be performed by a number of
      third-party GUI utilities, you should at least have a basic knowledge of
      <application>gsec</application>, since this is the official tool and it's present in every
      Firebird server installation. In the next sections you will use
      <application>gsec</application> to execute three tasks: changing the
      <database>SYSDBA</database> password, adding Firebird users and (optionally) appointing
      co-administrators. First though, some points of attention:</para>
      <variablelist>
        <varlistentry>
          <term>Permission to run gsec</term>
          <listitem>
            <para>With some Firebird installations, you can only run <application>gsec
            </application>if you are logged into the operating system as Superuser (<systemitem
            class="username">root</systemitem> on Linux) or as the user the Firebird server process
            runs under. On Windows server platforms, you typically need to be in the Power User
            group or higher to run <application>gsec</application> successfully.</para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term>Trouble running gsec</term>
          <listitem>
            <para>If you have enough privileges but invoking <application>gsec</application> results
            in a message like <quote><computeroutput>cannot attach to password database - unable to
            open database</computeroutput></quote>:</para>
            <itemizedlist>
              <listitem>
                <para>You may be running Firebird on Windows and for some reason the local protocol
                isn't working. One rather common cause for this is running Windows with Terminal
                Services (Remote Desktop Services) enabled and connecting to the server from a
                different session. To enable the local protocol, open
                <filename>firebird.conf</filename>, uncomment the <parameter>IpcName</parameter>
                parameter and set it to <literal>Global\FIREBIRD</literal>. Then restart the
                server.</para>
                <note>
                  <para>In Firebird 2.0.1 and up, Firebird automatically prepends
                  <literal>Global\</literal> to the IPCname if the connection fails because of
                  insufficient permissions, so this should not happen anymore.</para>
                </note>
              </listitem>
              <listitem>
                <para>If the above doesn't apply to you, you can at least circumvent the problem by
                <quote>tricking</quote> gsec into using TCP/IP. Add the following parameter to the
                command line, adjusting the path if necessary:</para>
                <blockquote>
                  <para><command>-database "localhost:C:\Program
                  Files\Firebird\Firebird_2_5\security2.fdb"</command></para>
                </blockquote>
                <para>The file <filename>security2.fdb</filename> is the <firstterm>security
                database</firstterm>, where Firebird keeps its user account details. It is located
                in your Firebird installation directory.</para>
              </listitem>
              <listitem>
                <para>Maybe your security database is a renamed <filename>security.fdb</filename>
                from Firebird 1.5 or earlier. Of course this can't be the case immediately after
                installation. Someone (you?) must have put it there, in order to keep the existing
                accounts available. Consult the Release Notes for instructions on how to upgrade old
                security databases.</para>
              </listitem>
            </itemizedlist>
            <para>If the error message starts with <quote><computeroutput>Cannot attach to services
            manager</computeroutput></quote>, the server may not be running at all. In that case, go
            back to <link linkend="qsg25-installing-testing"><citetitle>Testing your
            installation</citetitle></link> and fix the problem.</para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term>Invoking gsec on Linux</term>
          <listitem>
            <para>On *nix systems, if you call <application>gsec</application> from its own
            directory, you should type <command>./gsec</command> instead of just
            <command>gsec</command>. The current directory is usually not part of the search path,
            so plain <command>gsec</command> may either fail or launch a <quote>wrong</quote>
            <application>gsec</application>.</para>
          </listitem>
        </varlistentry>
      </variablelist>
      <section id="qsg25-config-gsec-changepw">
        <title><indexterm>
            <primary>SYSDBA</primary>
          </indexterm><indexterm>
            <primary>Passwords</primary>
            <secondary>changing</secondary>
          </indexterm>Changing the <database>SYSDBA</database> password</title>
        <para>One Firebird account is created automatically as part of the installation process:
        <database>SYSDBA</database>. This account has all the privileges on the server and cannot be
        deleted. Depending on version, OS, and architecture, the installation program will
        either</para>
        <itemizedlist>
          <listitem>
            <para>install the <database>SYSDBA</database> user with the password
            <literal>masterkey</literal> (actually, <literal>masterke</literal>: characters after
            the eighth are ignored), or</para>
          </listitem>
          <listitem>
            <para>ask you to enter a password during installation, or</para>
          </listitem>
          <listitem>
            <para>generate a random password and store that in the file
            <filename>SYSDBA.password</filename> within your Firebird installation directory.</para>
          </listitem>
        </itemizedlist>
        <para>If the password is <literal>masterkey</literal> and your server is exposed to the
        Internet <emphasis>at all</emphasis> – or even to a local network, unless you trust every
        user with the <database>SYSDBA</database> password – you should change it immediately using
        the <application>gsec</application> command-line utility. Go to a command shell,
        <emphasis>cd</emphasis> to the Firebird <filename class="directory">bin</filename>
        subdirectory and issue the following command to change the password to (as an example)
        <literal>icuryy4me</literal>:</para>
        <blockquote>
          <para><command>gsec -user sysdba -pass masterkey -mo sysdba -pw icuryy4me</command></para>
        </blockquote>
        <para>Notice that you specify <quote>sysdba</quote> twice in the command:</para>
        <itemizedlist>
          <listitem>
            <para>With the <parameter>-user</parameter> parameter you identify yourself as
            <database>SYSDBA</database>. You also provide <database>SYSDBA</database>'s current
            password in the <parameter>-pass</parameter> parameter.</para>
          </listitem>
          <listitem>
            <para>The <parameter>-mo[dify]</parameter> parameter tells gsec that you want to modify
            an account – which happens to be <database>SYSDBA</database> again. Lastly,
            <parameter>-pw</parameter> specifies the type of modification: the password.</para>
          </listitem>
        </itemizedlist>
        <para>If all has gone well, the new password <literal>icuryy4me</literal> is now encrypted
        and stored, and <literal>masterkey</literal> is no longer valid. Please be aware that unlike
        Firebird user names, passwords are case-sensitive.</para>
      </section>
      <section id="qsg25-config-gsec-adduser">
        <title><indexterm>
            <primary>User accounts</primary>
          </indexterm>Adding Firebird user accounts</title>
        <para>Firebird allows the creation of many different user accounts. Each of them can own
        databases and also have various types of access to databases and database objects it doesn't
        own.</para>
        <para>Using <application>gsec</application>, you can add a user account as follows from the
        command line in the Firebird <filename class="directory">bin</filename> subdirectory:</para>
        <blockquote>
          <para><command>gsec -user sysdba -pass masterkey -add billyboy -pw
          sekrit66</command></para>
        </blockquote>
        <para>Provided that you've supplied the correct password for <database>SYSDBA</database>, a
        user account called <systemitem class="username">billyboy</systemitem> will now have been
        created with password <systemitem>sekrit66</systemitem>. Remember that passwords are
        case-sensitive.</para>
        <para>Firebird 2.5 also introduces SQL commands for user management. While attached to any
        database, <database>SYSDBA</database> (or co-admins, see below) can create, alter and drop
        users like this:</para>
        <blockquote>
          <programlisting>create user sonny password 'cher_ie'
alter user sonny password '9hgf72354b'
drop user sonny</programlisting>
        </blockquote>
        <para>Other parameters for <database>CREATE/ALTER USER</database> are
        <database>FIRSTNAME</database>, <database>MIDDLENAME</database> and
        <database>LASTNAME</database>. Like <database>PASSWORD</database>, they all take a string
        argument.</para>
        <para>Ordinary Firebird users can alter their own account details with <application>gsec
        </application>(<quote><command>gsec -user toby -pass hEltoPay -mo toby -pw
        purgaToby</command></quote>) and with SQL (<quote>alter user toby password
        'purgaToby'</quote>). Only the account name itself can never be changed, not even by
        <database>SYSDBA</database>.</para>
      </section>
      <section id="qsg25-config-gsec-addadmin">
        <title><indexterm>
            <primary>Administrators</primary>
          </indexterm><indexterm>
            <primary><database>RDB$ADMIN</database> role</primary>
            <secondary>in the security database</secondary>
          </indexterm>Appointing co-administrators</title>
        <para><emphasis role="bold">Note:</emphasis> What follows here is not essential knowledge
        for beginners. You can skip it if you like and go on to the <link
        linkend="qsg25-config-security">Security</link> section.</para>
        <para>In Firebird 2.5 and up, <database>SYSDBA</database> (and others with administrator
        rights) can appoint co-administrators. In <application>gsec</application> this is done by
        adding the <parameter>-admin</parameter> parameter:</para>
        <blockquote>
          <para><command>gsec -user sysdba -pass masterkey -add bigbill -pw bigsekrit -admin
          yes</command></para>
          <para><command>gsec -user sysdba -pass masterkey -mo littlejohn -admin
          yes</command></para>
        </blockquote>
        <para>The first command creates user <systemitem class="username">bigbill</systemitem> as a
        Firebird administrator, who can add, alter and drop users. The second command grants
        administrator privileges to the existing user <systemitem
        class="username">littlejohn</systemitem>.</para>
        <para>The SQL equivalents of these commands are:</para>
        <blockquote>
          <programlisting>create user bigbill password 'bigsekrit' grant admin role
alter user littlejohn grant admin role</programlisting>
        </blockquote>
        <para>To revoke administrator privileges with <application>gsec</application>, use
        <parameter>-admin no</parameter>. In SQL, use <database>REVOKE ADMIN ROLE</database>.</para>
        <note>
          <title>Notes</title>
          <itemizedlist>
            <listitem>
              <para><database>GRANT ADMIN ROLE</database> and <database>REVOKE ADMIN ROLE</database>
              are not <database>GRANT</database> and <database>REVOKE</database> statements,
              although they look that way. They are parameters to the <database>CREATE</database>
              and <database>ALTER USER</database> statements. The actual role name involved here is
              <database>RDB$ADMIN</database>. This role also exists in regular databases; more about
              that in a minute.</para>
            </listitem>
            <listitem>
              <para>Every user who has received administrator rights can pass them on to others.
              Therefore, there is no explicit <database>WITH ADMIN OPTION</database>.</para>
            </listitem>
          </itemizedlist>
        </note>
        <section id="qsg25-config-gsec-addadmin-diff">
          <title>Differences between co-administrators and <database>SYSDBA</database></title>
          <itemizedlist>
            <listitem>
              <para>Co-admins can create, alter and drop users, but they have no automatic
              privileges in regular databases, like <database>SYSDBA</database> has.</para>
            </listitem>
            <listitem>
              <para>Unlike <database>SYSDBA</database>, co-admins must specify the extra parameter
              <parameter>-role rdb$admin</parameter> every time they invoke
              <application>gsec</application> to add, modify, drop or view users.</para>
            </listitem>
            <listitem>
              <para><indexterm>
                  <primary><database>RDB$ADMIN</database> role</primary>
                  <secondary>in regular databases</secondary>
                </indexterm>Co-admins who want to use the SQL user management commands must specify
              the <database>RDB$ADMIN</database> role when connecting. However, since nobody can
              connect to the security database, this requires a little trickery: the co-admin is
              granted the <database>RDB$ADMIN</database> role in at least one regular database as
              well. This is done in the usual way:</para>
              <blockquote>
                <programlisting>grant rdb$admin to bigbill</programlisting>
              </blockquote>
              <para>Grantors can be the database owner, <database>SYSDBA</database>, and every other
              user who has the <database>RDB$ADMIN</database> role in that database and has
              specified it while connecting. Every <database>RDB$ADMIN</database> member in a
              database can pass the role on to others, so again there is no <database>WITH ADMIN
              OPTION</database>. Once the co-admin has obtained the role, he can connect to the
              (regular) database with it and use the SQL user management commands. It's not the most
              elegant of solutions, but that's how it works.</para>
            </listitem>
          </itemizedlist>
          <note>
            <title>Please remember:</title>
            <para>The <database>RDB$ADMIN</database> role in a database gives the grantee
            <database>SYSDBA</database> rights <emphasis>in that database only</emphasis>!</para>
            <itemizedlist>
              <listitem>
                <para>If it is the security database, the grantee can manage user accounts, but has
                no special privileges in other databases.</para>
              </listitem>
              <listitem>
                <para>If it is a regular database, the grantee can control that database like he was
                <database>SYSDBA</database>, but again has no special privileges in other databases,
                and has no user administation privileges.</para>
              </listitem>
            </itemizedlist>
            <para>Of course it is possible to grant a user the <database>RDB$ADMIN</database> role
            in several databases, including the security database.</para>
          </note>
        </section>
      </section>
    </section>
    <section id="qsg25-config-security">
      <title><indexterm>
          <primary>Security</primary>
        </indexterm><indexterm>
          <primary>Databases</primary>
          <secondary>security</secondary>
        </indexterm>Security</title>
      <para>Firebird 2.5 offers a number of security options, designed to make unauthorised access
      as difficult as possible. Be warned however that some configurable security features default
      to the old, <quote>insecure</quote> behaviour inherited from InterBase and Firebird 1.0, in
      order not to break existing applications.</para>
      <para>It pays to familiarise yourself with Firebird's security-related configuration
      parameters. You can significantly enhance your system's security if you raise the protection
      level wherever possible. This is not only a matter of setting parameters, by the way: other
      measures involve tuning filesystem access permissions, an intelligent user accounts policy,
      etc.</para>
      <para>Below are some guidelines for protecting your Firebird server and databases.</para>
      <variablelist>
        <varlistentry>
          <term>Run Firebird as non-system user</term>
          <listitem>
            <para>On Unix-like systems, Firebird already runs as user <systemitem
            class="username">firebird</systemitem> by default, not as <systemitem
            class="username">root</systemitem>. On Windows server platforms, you can also run the
            Firebird service under a designated user account (e.g. <systemitem
            class="username">Firebird</systemitem>). The default practice – running the service as
            the <systemitem class="username">LocalSystem</systemitem> user – poses a security risk
            if your system is connected to the Internet. Consult <filename>README.instsvc</filename>
            in the <filename class="directory">doc</filename> subdir to learn more about
            this.</para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term><indexterm>
              <primary>SYSDBA</primary>
            </indexterm>Change <database>SYSDBA</database>'s password</term>
          <listitem>
            <para>As discussed before, if your Firebird server is reachable from the network and the
            system password is <literal>masterkey</literal>, change it.</para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term><indexterm>
              <primary>SYSDBA</primary>
            </indexterm>Don't create user databases as SYSDBA</term>
          <listitem>
            <para><database>SYSDBA</database> is a very powerful account, with full (destructive)
            access rights to all your Firebird databases. Its password should be known to a few
            trusted database administrators only. Therefore, you shouldn't use this super-account to
            create and populate regular databases. Instead, generate normal user accounts, and
            provide their account names and passwords to your users as needed. You can do this with
            <application>gsec</application> as shown above, or with any third-party Firebird
            administration tool.</para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term>Protect databases on the filesystem level</term>
          <listitem>
            <para>Anybody who has filesystem-level read access to a database file can copy it,
            install it on a system under his or her own control, and extract all data from it –
            including possibly sensitive information. Anybody who has filesystem-level write access
            to a database file can corrupt it or totally destroy it.</para>
            <para>As a rule, only the Firebird server process should have access to the database
            files. Users don't need, and should not have, access to the files – not even read-only.
            They query databases via the server, and the server makes sure that users only get the
            allowed type of access (if at all) to any objects within the database.</para>
            <para>An exception to this rule is the Windows Embedded Server, which
            <emphasis>requires</emphasis> that users have proper access rights to the database file
            itself.</para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term>Disable Classic local mode on Linux</term>
          <listitem>
            <para>Another exception to the above rule is the so-called local or embedded access mode
            of the Firebird Classic and SuperClassic servers on Linux. Here too, users
            <emphasis>must</emphasis> have proper access rights to the database file itself. They
            also need read access to the security database <filename>security2.fdb</filename>. If
            this worries you (and it probably should), reserve filesystem access to the security
            database (and other databases, while you're at it) to the server process only. Users are
            then obliged to connect via the network layer. Don't remove the <filename
            class="libraryfile">libfbembed</filename> library from your system, though: it contains
            the complete server engine used by your Classic or SuperClassic server!</para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term><indexterm>
              <primary>Aliases</primary>
            </indexterm><indexterm>
              <primary>Databases</primary>
              <secondary>aliases</secondary>
            </indexterm>Use database aliases</term>
          <listitem>
            <para><firstterm>Database aliases</firstterm> shield the client from physical database
            locations. Using aliases, a client can e.g. connect to
            <quote><literal>frodo:zappa</literal></quote> without having to know that the real
            location is
            <filename>frodo:/var/firebird/music/underground/mothers_of_invention.fdb</filename>.
            Aliases also allow you to relocate databases while the clients keep using their existing
            connection strings.</para>
            <para>Aliases are listed in the file <filename>aliases.conf</filename>, in this format
            on Windows machines:</para>
            <blockquote>
              <programlisting>poker = E:\Games\Data\PokerBase.fdb
blackjack.fdb = C:\Firebird\Databases\cardgames\blkjk_2.fdb</programlisting>
            </blockquote>
            <para>And on Linux:</para>
            <blockquote>
              <programlisting>books = /home/bookworm/database/books.fdb
zappa = /var/firebird/music/underground/mothers_of_invention.fdb</programlisting>
            </blockquote>
            <para>Giving the alias an <filename class="extension">.fdb</filename> (or any other)
            extension is fully optional. Of course if you do include it, you must also specify it
            when you use the alias to connect to the database.</para>
            <para>Aliases, once entered and saved, take effect immediately. There is no need to
            restart the server.</para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term>Restrict database access</term>
          <listitem>
            <para>The <parameter>DatabaseAccess</parameter> parameter in
            <filename>firebird.conf</filename> can be set to <constant>Restrict</constant> to limit
            access to explicitly listed filesystem trees, or even to <constant>None</constant> to
            allow access to aliased databases only. Default is <constant>All</constant>, i.e. no
            restrictions.</para>
            <para>Note that this is not the same thing as the filesystem-level access protection
            discussed earlier: when <parameter>DatabaseAccess</parameter> is anything other than
            <constant>All</constant>, the server will refuse to open any databases outside the
            defined scope even if it has sufficient rights on the database files.</para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term><indexterm>
              <primary>Authentication</primary>
            </indexterm>Choose your authentication model</term>
          <listitem>
            <para>Firebird supports three authentication models when connecting to databases or
            using the tools:</para>
            <orderedlist>
              <listitem>
                <para><emphasis>Native:</emphasis> The user must identify him/herself with a
                Firebird username and password, which the server checks against the security
                database.</para>
              </listitem>
              <listitem>
                <para><emphasis>Trusted:</emphasis> The user is automatically identified by his OS
                account name.</para>
              </listitem>
              <listitem>
                <para><emphasis>Mixed:</emphasis> The user either supplies a Firebird username and
                password, or is logged in with his OS account name.</para>
              </listitem>
            </orderedlist>
            <para>On Linux, the mixed model is in effect. On Windows, native authentication is the
            default, but you can change it to trusted or mixed by setting the
            <parameter>Authentication</parameter> parameter in
            <filename>firebird.conf</filename>.</para>
            <para>Depending on your Windows system configuration and the way Firebird is used,
            <emphasis>trusted</emphasis> may be the most secure option.</para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term><indexterm>
              <primary>Administrators</primary>
            </indexterm>Consider whether Windows administrators should have
          <database>SYSDBA</database> rights</term>
          <listitem>
            <para>In Firebird 2.1, if <parameter>Authentication</parameter> was
            <emphasis>trusted</emphasis> or <emphasis>mixed</emphasis>, Windows administrators would
            automatically receive <database>SYSDBA</database> privileges in all databases, including
            the security database. In Firebird 2.5, this is no longer the case. This reduces the
            risk that administrators with little or no Firebird knowledge mess up databases or user
            accounts.</para>
            <para>If you want to give individual administrators <database>SYSDBA</database> power in
            the security database and/or regular databases, you can grant them the
            <database>RDB$ADMIN</database> role as described in the section <link
            linkend="qsg25-config-gsec-addadmin">Appointing co-administrators</link>. If, on the
            other hand, you want to restore the automatic <database>SYSDBA</database> mapping as it
            was in Firebird 2.1, read the following instructions.</para>
            <para>To give all the administrators automatic <database>SYSDBA</database> rights in the
            security database so they can manage Firebird user accounts, give the command:</para>
            <blockquote>
              <para><command>gsec -user sysdba -pass masterkey -mapping set</command></para>
            </blockquote>
            <para>You must do this as <database>SYSDBA</database> - a co-admin account won't do. To
            reverse the command, use <parameter>-mapping drop</parameter>.</para>
            <para>To give all the administrators <database>SYSDBA</database> rights in an ordinary
            database, log into the database as the owner, <database>SYSDBA</database> or someone who
            has the <database>RDB$ADMIN</database> role in that database, and issue the following
            SQL statement:</para>
            <blockquote>
              <programlisting>alter role rdb$admin set auto admin mapping</programlisting>
            </blockquote>
            <para>You must repeat this in every database where you want Windows administrators to
            have automatic <database>SYSDBA</database> rights. To turn the mapping off again, use
            <database>DROP</database> instead of <database>SET</database>.</para>
            <para>If automatic mapping is on, Windows administrators must <emphasis>not</emphasis>
            specify the <database>RDB$ADMIN</database> role when invoking
            <application>gsec</application> or connecting to a database – at least not if they want
            to make use of their <database>SYSDBA</database> rights. If they specify
            <emphasis>any</emphasis> role at all – even an unexisting one – the automatic mapping
            will not work.</para>
            <!--Specifying a role will lead to a standard role check. Since the admistrator (in most cases) isn't an explicit member of role RDB$ADMIN,
as registered in RDB$USER_PRIVILEGES, the role wil be set to NONE. If no role is specified, a check will be made if auto admin mapping
is on. If yes *and* the user is a Windows admin, the role will be set to RDB$ADMIN. You should check this with CURRENT_ROLE, the 'ROLE'
shown by isql after a connect just copies what the user has specified, regardless of failure or success.-->
          </listitem>
        </varlistentry>
      </variablelist>
      <para>There are more security parameters, but the ones not mentioned here are already set to
      an adequate protection level by default. You can read about them in the 1.5 through 2.5
      Release Notes and in the comments in <filename>firebird.conf</filename> itself.</para>
    </section>
    <section id="qsg25-config-cpa">
      <title><indexterm>
          <primary>Control Panel applets</primary>
        </indexterm>Windows Control Panel applets</title>
      <para>Several control panel applets are available for use with Firebird. Whilst such applets
      are not essential, they do provide a convenient way to start and stop the server and check its
      current status.</para>
      <section id="qsg25-config-cpa-fbservermanager">
        <title>Firebird Server Manager</title>
        <para>The Firebird Server Manager applet is included in the Firebird distribution. The
        option to install this applet is only available for Superserver.</para>
        <note>
          <para>The applet is also usable for (Super)Classic, provided that Firebird runs as a
          service, not as an application. Since the installation dialogue won't give you the option
          to include the applet with a Classic server, you must, if you really want it:</para>
          <itemizedlist spacing="compact">
            <listitem>
              <para>install Superserver first;</para>
            </listitem>
            <listitem>
              <para>copy the applet <filename>Firebird2Control.cpl</filename> from the Windows
              system folder to a safe place;</para>
            </listitem>
            <listitem>
              <para>uninstall Superserver;</para>
            </listitem>
            <listitem>
              <para>install Classic;</para>
            </listitem>
            <listitem>
              <para>copy the applet back to the system directory.</para>
            </listitem>
          </itemizedlist>
        </note>
        <para>This is a screenshot of the activated applet. Notice that the title bar says
        <quote>Firebird Server Control</quote>, although it is listed in the Control Panel as
        <citetitle>Firebird Server Manager</citetitle>.</para>
        <mediaobject>
          <imageobject>
            <imagedata align="center" fileref="images/FbServerControl-2.png" format="PNG" />
          </imageobject>
        </mediaobject>
        <para>This applet only works on Windows NT, 2000/3/8, XP, Vista and 7.</para>
      </section>
      <section id="qsg25-config-cpa-fbcc">
        <title>Firebird Control Center</title>
        <para>For an alternative to the bundled applet, you can visit this webpage:</para>
        <blockquote>
          <para><ulink
          url="http://www.achim-kalwa.de/fbcc.phtml">http://www.achim-kalwa.de/fbcc.phtml</ulink></para>
        </blockquote>
        <para>...and download the Firebird Control Center (FBCC). Please note that, unlike the
        applet included with Firebird, the Firebird Control Center will <emphasis>not</emphasis>
        work with Classic or SuperClassic servers. This may change in the future.</para>
        <para>The current version – 0.4.2 – should work well under Windows 2000 and up. It offers
        the same functionality as Firebird's own applet, and more. An older release, still
        downloadable at <ulink
        url="http://www.achim-kalwa.de/dl/fbcc-0.2.7.exe">http://www.achim-kalwa.de/dl/fbcc-0.2.7.exe</ulink>,
        also runs under Windows 9x, ME and NT. Notice however that these Windows versions are no
        longer actively supported by the Firebird project, even if the engine runs on it.</para>
      </section>
    </section>
    <section id="qsg25-config-admintools">
      <title><indexterm>
          <primary>Admin tools</primary>
        </indexterm>Administration tools</title>
      <para>The Firebird kit does not come with a GUI admin tool. It does have a set of command-line
      tools – executable programs which are located in the <filename
      class="directory">bin</filename> subdirectory of your Firebird installation. One of them,
      <application>gsec</application>, has already been introduced to you.</para>
      <para>The range of excellent GUI tools available for use with a Windows client machine is too
      numerous to describe here. A few GUI tools written in Borland Kylix, for use on Linux client
      machines, are also in various stages of completion.</para>
      <para>Inspect the <ulink url="http://www.ibphoenix.com/download/tools/admin">Downloads &gt;
      Tools &gt; Administration page</ulink> at <ulink
      url="http://www.ibphoenix.com">http://www.ibphoenix.com</ulink> for all of the options.</para>
      <note>
        <para>Remember: you can use a Windows client to access a Linux server and vice-versa.</para>
      </note>
    </section>
  </section>
  <section id="qsg25-databases">
    <title><indexterm>
        <primary>Databases</primary>
        <secondary>working with databases</secondary>
      </indexterm>Working with databases</title>
    <para>In this part of the manual you will learn:</para>
    <itemizedlist spacing="compact">
      <listitem>
        <para>how to connect to an existing database,</para>
      </listitem>
      <listitem>
        <para>how to create a database,</para>
      </listitem>
      <listitem>
        <para>and some things you should know about Firebird SQL.</para>
      </listitem>
    </itemizedlist>
    <para>In as much as remote connections are involved, we will use the recommended TCP/IP
    protocol.</para>
    <section id="qsg25-databases-connstrings">
      <title><indexterm>
          <primary>Connecting</primary>
          <secondary>connection strings</secondary>
        </indexterm>Connection strings</title>
      <para>If you want to connect to a database or create one you have to supply, amongst other
      things, a <firstterm>connection string</firstterm> to the client application (or, if you are a
      programmer, to the routines you are calling). A connection string uniquely identifies the
      location of the database on your computer, local network, or even the Internet.</para>
      <section id="qsg25-databases-connstrings-local">
        <title>Local connection strings</title>
        <para>An explicit local connection string consists of the path + filename specification in
        the native format of the filesystem used on the server machine, for example</para>
        <itemizedlist>
          <listitem>
            <para>on a Linux or other Unix-like server:</para>
            <blockquote>
              <para><filename>/opt/firebird/examples/empbuild/employee.fdb</filename></para>
            </blockquote>
          </listitem>
          <listitem>
            <para>on a Windows server:</para>
            <blockquote>
              <para><filename>C:\Biology\Data\Primates\Apes\populations.fdb</filename></para>
            </blockquote>
          </listitem>
        </itemizedlist>
        <para>Many clients also allow relative path strings (e.g.
        <quote><filename>..\examples\empbuild\employee.fdb</filename></quote>) but you should use
        them with caution, as it's not always obvious how they will be expanded. Getting an error
        message is annoying enough, but applying changes to another database than you thought you
        were connected to may be disastrous.</para>
        <indexterm>
          <primary>Aliases</primary>
        </indexterm>
        <indexterm>
          <primary>Databases</primary>
          <secondary>aliases</secondary>
        </indexterm>
        <para>Instead of a file path, the local connection string may also be a <emphasis>database
        alias</emphasis> that is defined in <filename>aliases.conf</filename>, as mentioned earlier.
        The format of the alias depends only on how it's defined in the aliases file, not on the
        server filesystem. Examples are:</para>
        <itemizedlist spacing="compact">
          <listitem>
            <para><filename>zappa</filename></para>
          </listitem>
          <listitem>
            <para><filename>blackjack.fdb</filename></para>
          </listitem>
          <listitem>
            <para><filename>poker</filename></para>
          </listitem>
        </itemizedlist>
        <tip>
          <para>If your local connections fail, it may be because the local protocol isn't working
          properly on your machine. If you're running Windows Vista, 2003 or XP with terminal
          services enabled, this can often be fixed by setting <parameter>IpcName</parameter> to
          <literal>Global\FIREBIRD</literal> in the configuration file
          <filename>firebird.conf</filename> (don't forget to uncomment the parameter and restart
          the server).</para>
          <para>If setting <parameter>IpcName</parameter> doesn't help and you don't get the local
          protocol enabled, you can always work around the problem by putting
          <quote><literal>localhost:</literal></quote> before your database paths or aliases, thus
          turning them into TCP/IP connection strings (discussed below).</para>
        </tip>
      </section>
      <section id="qsg25-databases-connstrings-tcpip">
        <title><indexterm>
            <primary>Server name and path</primary>
          </indexterm><indexterm>
            <primary>TCP/IP</primary>
          </indexterm>TCP/IP connection strings</title>
        <para>A TCP/IP connection string consists of:</para>
        <orderedlist spacing="compact">
          <listitem>
            <para>a server name or IP address</para>
          </listitem>
          <listitem>
            <para>a colon (<quote><literal>:</literal></quote>)</para>
          </listitem>
          <listitem>
            <para>either the absolute path + filename on the server machine, or an alias defined on
            the server machine.</para>
          </listitem>
        </orderedlist>
        <para>Examples:</para>
        <itemizedlist>
          <listitem>
            <para>On Linux/Unix:</para>
            <blockquote>
              <para><literal>pongo:/opt/firebird/examples/empbuild/employee.fdb</literal></para>
              <para><literal>bongo:fury</literal></para>
              <para><literal>112.179.0.1:/var/Firebird/databases/butterflies.fdb</literal></para>
              <para><literal>localhost:blackjack.fdb</literal></para>
            </blockquote>
          </listitem>
          <listitem>
            <para>On Windows:</para>
            <blockquote>
              <para><filename>siamang:C:\Biology\Data\Primates\Apes\populations.fdb</filename></para>
              <para><literal>sofa:D:\Misc\Friends\Rich\Lenders.fdb</literal></para>
              <para><literal>127.0.0.1:Borrowers</literal></para>
            </blockquote>
          </listitem>
        </itemizedlist>
        <para><indexterm>
            <primary>Aliases</primary>
          </indexterm><indexterm>
            <primary>Databases</primary>
            <secondary>aliases</secondary>
          </indexterm>Notice how the aliased connection strings don't give any clue about the server
        OS. And they don't have to, either: you talk to a Linux Firebird server just like you talk
        to a Windows Firebird server. In fact, specifying an explicit database path is one of the
        rare occasions where you have to be aware of the difference.</para>
      </section>
      <section id="qsg25-databases-connstrings-3rdpartyprogs">
        <title>Third-party programs</title>
        <para>Please note that some third-party client programs may have different requirements for
        the composition of connection strings. Refer to their documentation or online help to find
        out.</para>
      </section>
    </section>
    <section id="qsg25-databases-connecting">
      <title><indexterm>
          <primary>Connecting</primary>
        </indexterm><indexterm>
          <primary>Databases</primary>
          <secondary>connecting</secondary>
        </indexterm>Connecting to an existing database</title>
      <para><indexterm>
          <primary>Databases</primary>
          <secondary>example database</secondary>
        </indexterm><indexterm>
          <primary>Sample database</primary>
        </indexterm><indexterm>
          <primary>Example database</primary>
        </indexterm>A sample database named <filename>employee.fdb</filename> is located in the
      <filename class="directory">examples/empbuild</filename> subdirectory of your Firebird
      installation. You can use this database to <quote>try your wings</quote>.</para>
      <para>If you move or copy the sample database, be sure to place it on a hard disk that is
      physically attached to your server machine. Shares, mapped drives or (on Unix) mounted SMB
      (Samba) filesystems will not work. The same rule applies to any databases that you create or
      use.</para>
      <para>Connecting to a Firebird database requires the user to authenticate with a user name and
      a valid password. In order to work with objects inside the database – such as tables, views,
      etc. – you also need explicit permissions on those objects, unless you own them (you own an
      object if you have created it) or if you're connected as <database
      class="name">SYSDBA</database>. In the example database <filename>employee.fdb</filename>,
      sufficient permissions have been granted to <database>PUBLIC</database> (i.e. anybody who
      cares to connect) to enable you to view and modify data to your heart's content.</para>
      <para>For simplicity here, we will look at authenticating as <database
      class="name">SYSDBA</database> using the password <literal>masterkey</literal>. Also, to keep
      the lines in the examples from running off the right edge, we will work with local databases
      and use relative paths. Of course everything you'll learn in these sections can also be
      applied to remote databases, simply by supplying a full TCP/IP connection string.</para>
      <section id="qsg25-databases-connecting-isql">
        <title><indexterm>
            <primary>isql</primary>
            <secondary>connecting to a database</secondary>
          </indexterm><indexterm>
            <primary>Databases</primary>
            <secondary>connecting</secondary>
            <tertiary>with isql</tertiary>
          </indexterm>Connecting with <application>isql</application></title>
        <para>Firebird ships with a text-mode client named <firstterm>isql</firstterm> (Interactive
        SQL utility). You can use it in several ways to connect to a database. One of them, shown
        below, is to start it in interactive mode. Go to the <filename
        class="directory">bin</filename> subdirectory of your Firebird installation and type
        <command>isql</command> (Windows) or <command>./isql</command> (Linux) at the command
        prompt.</para>
        <para>[In the following examples, ↵ means <quote>hit <keycap>Enter</keycap></quote>]</para>
        <indexterm>
          <primary>Connecting</primary>
          <secondary>CONNECT statement</secondary>
        </indexterm>
        <indexterm class="singular" scope="local">
          <primary>SQL</primary>
          <secondary>CONNECT statement</secondary>
        </indexterm>
        <screen>C:\Program Files\Firebird\Firebird_2_5\bin&gt;isql↵
Use CONNECT or CREATE DATABASE to specify a database
SQL&gt;CONNECT ..\examples\empbuild\employee.fdb user SYSDBA password masterkey;↵</screen>
        <important>
          <itemizedlist>
            <listitem>
              <para>In <application>isql</application>, every SQL statement must end with a
              semicolon. If you hit <keycap>Enter</keycap> and the line doesn't end with a
              semicolon, isql assumes that the statement continues on the next line and the prompt
              will change from <prompt>SQL&gt;</prompt> to <prompt>CON&gt;</prompt>. This enables
              you to split long statements over multiple lines. If you hit <keycap>Enter</keycap>
              after your statement and you've forgotten the semicolon, just type it after the
              <prompt>CON&gt;</prompt> prompt on the next line and press <keycap>Enter</keycap>
              again.</para>
            </listitem>
            <listitem>
              <para>If you run a (Super)Classic Server on Linux, a fast, direct local connection is
              attempted if the database path does not start with a hostname. This may fail if your
              Linux login doesn't have sufficient access rights to the database file. In that case,
              connect to <literal>localhost:</literal><replaceable>&lt;path&gt;</replaceable>. Then
              the server process (usually running as user <systemitem
              class="username">firebird</systemitem>) will open the file. On the other hand,
              network-style connections may fail if a user created the database in Classic local
              mode and the server doesn't have enough access rights.</para>
            </listitem>
          </itemizedlist>
        </important>
        <note>
          <para>You can optionally enclose the path, the user name and/or the password in single
          (<literal>'</literal>) or double (<literal>"</literal>) quotes. If the path contains
          spaces, quoting is mandatory.</para>
        </note>
        <para>At this point, <application>isql</application> will inform you that you are
        connected:</para>
        <screen>Database:  ..\examples\empbuild\employee.fdb, User: sysdba
SQL&gt;</screen>
        <para>You can now continue to play about with the <filename>employee.fdb</filename>
        database. With <application>isql</application> you can query data, get information about the
        metadata, create database objects, run data definition scripts and much more.</para>
        <para>To get back to the command prompt, type:</para>
        <screen>SQL&gt;QUIT;↵</screen>
        <para>You can also type <literal>EXIT</literal> instead of <literal>QUIT</literal>, the
        difference being that <literal>EXIT</literal> will first commit any open transactions,
        making your modifications permanent.</para>
      </section>
      <section id="qsg25-databases-connecting-guiclient">
        <title><indexterm>
            <primary>Databases</primary>
            <secondary>connecting</secondary>
            <tertiary>with a GUI client</tertiary>
          </indexterm>Connecting with a GUI client</title>
        <para>GUI client tools usually take charge of composing the <database>CONNECT</database>
        string for you, using server, path (or alias), user name and password information that you
        type into prompting fields. Use the elements as described in the preceding topic.</para>
        <note>
          <title>Notes</title>
          <itemizedlist spacing="compact">
            <listitem>
              <para>It is quite common for such tools to expect the entire server + path/alias as a
              single connection string – just like <application>isql</application> does.</para>
            </listitem>
            <listitem>
              <para>Remember that file names and commands on Linux and other <quote>Unix-ish</quote>
              platforms are case-sensitive.</para>
            </listitem>
          </itemizedlist>
        </note>
      </section>
    </section>
    <section id="qsg25-databases-creating">
      <title><indexterm>
          <primary>isql</primary>
          <secondary>creating a database</secondary>
        </indexterm><indexterm>
          <primary>Databases</primary>
          <secondary>creating with isql</secondary>
        </indexterm>Creating a database using <application>isql</application></title>
      <para>There is more than one way to create a database with <application>isql</application>.
      Here, we will look at one simple way to create a database interactively – although, for your
      serious database definition work, you should create and maintain your metadata objects using
      data definition scripts.</para>
      <section id="qsg25-databases-creating-startisql">
        <title>Starting <application>isql</application></title>
        <para>To create a database interactively using the <application>isql</application> command
        shell, get to a command prompt in Firebird's <filename class="directory">bin</filename>
        subdirectory and type <command>isql</command> (Windows) or <command>./isql</command>
        (Linux):</para>
        <para>[In the following examples, ↵ means <quote>hit <keycap>Enter</keycap></quote>]</para>
        <screen>C:\Program Files\Firebird\Firebird_2_5\bin&gt;isql↵
Use CONNECT or CREATE DATABASE to specify a database</screen>
      </section>
      <section id="qsg25-databases-creating-creatdbstatement">
        <title><indexterm>
            <primary>CREATE DATABASE statement</primary>
          </indexterm><indexterm class="singular" scope="local">
            <primary>SQL</primary>
            <secondary>CREATE DATABASE statement</secondary>
          </indexterm>The CREATE DATABASE statement</title>
        <para>Now you can create your new database interactively. Let's suppose that you want to
        create a database named <filename>test.fdb</filename> and store it in a directory named
        <filename class="directory">data</filename> on your <systemitem
        class="filesystem">D</systemitem> drive:</para>
        <screen>SQL&gt;CREATE DATABASE 'D:\data\test.fdb' page_size 8192↵
CON&gt;user 'SYSDBA' password 'masterkey';↵</screen>
        <important>
          <itemizedlist>
            <listitem>
              <para>In the <database>CREATE DATABASE</database> statement it is
              <emphasis>mandatory</emphasis> to place quote characters (single or double) around
              path, username and password. This is different from the <database>CONNECT</database>
              statement.</para>
            </listitem>
            <listitem>
              <para>If you run a (Super)Classic Server on Linux and you don't start the database
              path with a hostname, creation of the database file is attempted with your Linux login
              as the owner. This may or may not be what you want (think of access rights if you want
              others to be able to connect). If you prepend <systemitem
              class="systemname">localhost:</systemitem> to the path, the server process (usually
              running as user <systemitem class="username">firebird</systemitem>) will create and
              own the file.</para>
            </listitem>
          </itemizedlist>
        </important>
        <para>The database will be created and, after a few moments, the SQL prompt will reappear.
        You are now connected to the new database and can proceed to create some test objects in
        it.</para>
        <para>But to verify that there really is a database there, let's first type in this
        query:</para>
        <screen>SQL&gt;SELECT * FROM RDB$RELATIONS;↵</screen>
        <para><indexterm>
            <primary>Databases</primary>
            <secondary>system tables</secondary>
          </indexterm><indexterm>
            <primary>Databases</primary>
            <secondary>metadata</secondary>
          </indexterm><indexterm>
            <primary>System tables</primary>
          </indexterm>Although you haven't created any tables yet, the screen will fill up with a
        large amount of data! This query selects all of the rows in the system table
        <database>RDB$RELATIONS</database>, where Firebird stores the metadata for tables. An
        <quote>empty</quote> database is not really empty: it contains a number of system tables and
        other objects. The system tables will grow as you add more user objects to your
        database.</para>
        <para>To get back to the command prompt type <literal>QUIT</literal> or
        <literal>EXIT</literal>, as explained in the section on connecting.</para>
      </section>
    </section>
    <section id="qsg25-databases-sql">
      <title><indexterm class="singular" scope="local">
          <primary>SQL</primary>
        </indexterm><indexterm class="singular" scope="local">
          <primary>Firebird SQL</primary>
        </indexterm>Firebird SQL</title>
      <para>Every database management system has its own idiosyncrasies in the ways it implements
      SQL. Firebird adheres to the SQL standard more rigorously than most other RDBMSes. Developers
      migrating from products that are less standards-compliant often wrongly suppose that Firebird
      is quirky, whereas many of its apparent quirks are not quirky at all.</para>
      <section id="qsg25-integer-division">
        <title><indexterm>
            <primary>Integer division</primary>
          </indexterm>Division of an integer by an integer</title>
        <para>Firebird accords with the SQL standard by truncating the result (quotient) of an
        integer/integer calculation to the next lower integer. This can have bizarre results unless
        you are aware of it.</para>
        <para>For example, this calculation is correct in SQL:</para>
        <blockquote>
          <para><literal>1 / 3 = 0</literal></para>
        </blockquote>
        <para>If you are upgrading from an RDBMS which resolves integer/integer division to a float
        quotient, you will need to alter any affected expressions to use a float or scaled numeric
        type for either dividend, divisor, or both.</para>
        <para>For example, the calculation above could be modified thus in order to produce a
        non-zero result:</para>
        <blockquote>
          <para><literal>1.000 / 3 = 0.333</literal></para>
        </blockquote>
      </section>
      <section id="qsg25-strings">
        <title><indexterm>
            <primary>Strings</primary>
          </indexterm>Things to know about strings</title>
        <section id="qsg25-strings-delimiter">
          <title><indexterm>
              <primary>Strings</primary>
              <secondary>delimiter symbol</secondary>
            </indexterm>String delimiter symbol</title>
          <para>Strings in Firebird are delimited by a pair of single quote (apostrophe) symbols:
          <literal>'I am a string'</literal> (ASCII code 39, <emphasis>not</emphasis> 96). If you
          used earlier versions of Firebird's relative, InterBase®, you might recall that double and
          single quotes were interchangeable as string delimiters. Double quotes cannot be used as
          string delimiters in Firebird SQL statements.</para>
        </section>
        <section id="qsg25-strings-apostrophes">
          <title><indexterm>
              <primary>Apostrophes in strings</primary>
            </indexterm><indexterm>
              <primary>Strings</primary>
              <secondary>apostrophes in strings</secondary>
            </indexterm>Apostrophes in strings</title>
          <para>If you need to use an apostrophe inside a Firebird string, you can
          <quote>escape</quote> the apostrophe character by preceding it with another
          apostrophe.</para>
          <para>For example, this string will give an error:</para>
          <blockquote>
            <para><literal>'Joe's Emporium'</literal></para>
          </blockquote>
          <para>because the parser encounters the apostrophe and interprets the string as
          <literal>'Joe'</literal> followed by some unknown keywords. To make it a legal string,
          double the apostrophe character:</para>
          <blockquote>
            <para><literal>'Joe''s Emporium'</literal></para>
          </blockquote>
          <para>Notice that this is TWO single quotes, not one double-quote.</para>
        </section>
        <section id="qsg25-strings-concat">
          <title><indexterm>
              <primary>Strings</primary>
              <secondary>concatenation</secondary>
            </indexterm>Concatenation of strings</title>
          <para>The concatenation symbol in SQL is two <quote>pipe</quote> symbols (ASCII 124, in a
          pair with no space between). In SQL, the <quote>+</quote> symbol is an arithmetic operator
          and it will cause an error if you attempt to use it for concatenating strings. The
          following expression prefixes a character column value with the string
          <quote><literal>Reported by: </literal></quote>:</para>
          <blockquote>
            <para><literal>'Reported by: ' || LastName</literal></para>
          </blockquote>
          <para>Firebird will raise an error if the result of a string concatenation exceeds the
          maximum (var)char size of 32 Kb. If only the <emphasis>potential</emphasis> result – based
          on variable or field size – is too long you'll get a warning, but the operation will be
          completed successfully. (In pre-2.0 Firebird, this too would cause an error and halt
          execution.)</para>
          <para>See also the section below, <xref linkend="qsg25-nulls" />, about concatenating in
          expressions involving <constant>NULL</constant>.</para>
        </section>
        <section id="qsg25-strings-quoted-identifiers">
          <title><indexterm>
              <primary>Double-quoted identifiers</primary>
            </indexterm>Double-quoted identifiers</title>
          <para>Before the SQL-92 standard, it was not legal to have object names (identifiers) in a
          database that duplicated keywords in the language, were case-sensitive or contained
          spaces. SQL-92 introduced a single new standard to make any of them legal, provided that
          the identifiers were defined within pairs of double-quote symbols (ASCII 34) and were
          always referred to using double-quote delimiters.</para>
          <para>The purpose of this <quote>gift</quote> was to make it easier to migrate metadata
          from non-standard RDBMSes to standards-compliant ones. The down-side is that, if you
          choose to define an identifier in double quotes, its case-sensitivity and the enforced
          double-quoting will remain mandatory.</para>
          <para>Firebird does permit a slight relaxation under a very limited set of conditions. If
          the identifier which was defined in double-quotes:</para>
          <orderedlist spacing="compact">
            <listitem>
              <para>was defined as all upper-case,</para>
            </listitem>
            <listitem>
              <para>is not a keyword, and</para>
            </listitem>
            <listitem>
              <para>does not contain any spaces,</para>
            </listitem>
          </orderedlist>
          <para>...then it can be used in SQL unquoted and case-insensitively. (But as soon as you
          put double-quotes around it, you must match the case again!)</para>
          <warning>
            <para>Don't get too smart with this! For instance, if you have tables
            "<database>TESTTABLE</database>" and "<database>TestTable</database>", both defined
            within double-quotes, and you issue the command:</para>
            <blockquote>
              <screen>SQL&gt;select * from TestTable;</screen>
            </blockquote>
            <para>...you will get the records from "<database>TESTTABLE</database>", not
            "<database>TestTable</database>"!</para>
          </warning>
          <para>Unless you have a compelling reason to define quoted identifiers, it is usually
          recommended that you avoid them. Firebird happily accepts a mix of quoted and unquoted
          identifiers – so there is no problem including that keyword which you inherited from a
          legacy database, if you need to.</para>
          <warning>
            <para>Some database admin tools enforce double-quoting of <emphasis>all</emphasis>
            identifiers by default. Try to choose a tool which makes double-quoting optional.</para>
          </warning>
        </section>
      </section>
      <section id="qsg25-nulls" xreflabel="Expressions involving NULL">
        <title><indexterm>
            <primary>NULL</primary>
          </indexterm>Expressions involving <constant>NULL</constant></title>
        <para>In SQL, <constant>NULL</constant> is not a value. It is a condition, or
        <emphasis>state</emphasis>, of a data item, in which its value is unknown. Because it is
        unknown, <constant>NULL</constant> cannot behave like a value. When you try to perform
        arithmetic on <constant>NULL</constant>, or involve it with values in other expressions, the
        result of the operation will almost always be <constant>NULL</constant>. It is not zero or
        blank or an <quote>empty string</quote> and it does not behave like any of these
        values.</para>
        <para>Below are some examples of the types of surprises you will get if you try to perform
        calculations and comparisons with <constant>NULL</constant>.</para>
        <para>The following expressions all return <constant>NULL</constant>:</para>
        <itemizedlist>
          <listitem>
            <para><literal>1 + 2 + 3 + <constant>NULL</constant></literal></para>
          </listitem>
          <listitem>
            <para><literal>not (<constant>NULL</constant>)</literal></para>
          </listitem>
          <listitem>
            <para><literal>'Home ' || 'sweet ' || <constant>NULL</constant></literal></para>
          </listitem>
        </itemizedlist>
        <para>You might have expected 6 from the first expression and <quote><literal>Home sweet
        </literal></quote> from the third, but as we just said, <constant>NULL</constant> is not
        like the number 0 or an empty string – it's far more destructive!</para>
        <para>The following expression:</para>
        <itemizedlist>
          <listitem>
            <para><literal>FirstName || ' ' || LastName</literal></para>
          </listitem>
        </itemizedlist>
        <para>will return <constant>NULL</constant> if either <varname>FirstName</varname> or
        <varname>LastName</varname> is <constant>NULL</constant>. Otherwise it will nicely
        concatenate the two names with a space in between – even if any one of the variables is an
        empty string.</para>
        <tip>
          <para>Think of <constant>NULL</constant> as <constant>UNKNOWN</constant> and these strange
          results suddenly start to make sense! If the value of <varname>Number</varname> is
          unknown, the outcome of '<literal>1 + 2 + 3 + Number</literal>' is also unknown (and
          therefore <constant>NULL</constant>). If the content of <varname>MyString</varname> is
          unknown, then so is '<literal>MyString || YourString</literal>' (even if
          <varname>YourString</varname> is non-<constant>NULL</constant>). Etcetera.</para>
        </tip>
        <para>Now let's examine some PSQL (Procedural SQL) examples with
        <code>if</code>-constructs:</para>
        <itemizedlist>
          <listitem>
            <para><programlisting>if (a = b) then
  MyVariable = 'Equal';
else
  MyVariable = 'Not equal';</programlisting></para>
            <para>After executing this code, <varname>MyVariable</varname> will be <literal>'Not
            equal'</literal> if both <varname>a</varname> and <varname>b</varname> are
            <constant>NULL</constant>. The reason is that <literal>'a = b'</literal> yields
            <constant>NULL</constant> if at least one of them is <constant>NULL</constant>. If the
            test expression of an <quote><literal>if</literal></quote> statement is
            <constant>NULL</constant>, it behaves like <literal>false</literal>: the
            '<literal>then</literal>' block is skipped, and the '<literal>else</literal>' block
            executed.</para>
            <warning>
              <para>Although the expression may <emphasis>behave</emphasis> like
              <literal>false</literal> in this case, it's still <constant>NULL</constant>. If you
              try to invert it using <function>not()</function>, what you get is another
              <constant>NULL</constant> – not <quote><literal>true</literal></quote>.</para>
            </warning>
          </listitem>
          <listitem>
            <para><programlisting>if (a &lt;&gt; b) then
  MyVariable = 'Not equal';
else
  MyVariable = 'Equal';</programlisting></para>
            <para>Here, <varname>MyVariable</varname> will be <literal>'Equal'</literal> if
            <varname>a</varname> is <constant>NULL</constant> and <varname>b</varname> isn't, or
            vice versa. The explanation is analogous to that of the previous example.</para>
          </listitem>
        </itemizedlist>
        <section id="qsg25-nulls-distinct">
          <title>The DISTINCT keyword comes to the rescue!</title>
          <para>Firebird 2 and above implement a new use of the <database>DISTINCT</database>
          keyword allowing you to perform (in)equality tests that take <constant>NULL</constant>
          into account. The semantics are as follows:</para>
          <itemizedlist spacing="compact">
            <listitem>
              <para>Two expressions are <database>DISTINCT</database> if they have different values
              or if one is <constant>NULL</constant> and the other isn't;</para>
            </listitem>
            <listitem>
              <para>They are <database>NOT DISTINCT</database> if they have the same value or if
              both are <constant>NULL</constant>.</para>
            </listitem>
          </itemizedlist>
          <para>Notice that if neither operand is <constant>NULL</constant>,
          <database>DISTINCT</database> works exactly like the
          <quote><literal>&lt;&gt;</literal></quote> operator, and <database>NOT DISTINCT</database>
          like the <quote><literal>=</literal></quote> operator.</para>
          <para><database>DISTINCT</database> and <database>NOT DISTINCT</database> always return
          <constant>true</constant> or <constant>false</constant>, never
          <constant>NULL</constant>.</para>
          <para><database>Using DISTINCT, you can rewrite the first PSQL example as
          follows:</database></para>
          <programlisting>if (a is not distinct from b) then
  MyVariable = 'Equal';
else
  MyVariable = 'Not equal';</programlisting>
          <para>And the second as:</para>
          <programlisting>if (a is distinct from b) then
  MyVariable = 'Not equal';
else
  MyVariable = 'Equal';</programlisting>
          <para>These versions will give you the results that a normal (i.e. not SQL-brainwashed)
          human being would expect, whether there are <constant>NULL</constant>s involved or
          not.</para>
        </section>
        <section id="qsg25-nulls-more">
          <title>More about <constant>NULL</constant>s</title>
          <para>A lot more information about <constant>NULL</constant> behaviour can be found in the
          <citetitle>Firebird Null Guide</citetitle>, at these locations:</para>
          <blockquote>
            <para><ulink
            url="http://www.firebirdsql.org/manual/nullguide.html">http://www.firebirdsql.org/manual/nullguide.html</ulink>
            (HTML)</para>
            <para><ulink
            url="http://www.firebirdsql.org/pdfmanual/Firebird-Null-Guide.pdf">http://www.firebirdsql.org/pdfmanual/Firebird-Null-Guide.pdf</ulink>
            (PDF)</para>
            <!--Both links still valid on new site.-->
          </blockquote>
        </section>
      </section>
    </section>
  </section>
  <section id="qsg25-safety">
    <title>Protecting your data</title>
    <section id="qsg25-safety-backup">
      <title><indexterm>
          <primary>Backup</primary>
        </indexterm><indexterm>
          <primary>Restore</primary>
        </indexterm><indexterm>
          <primary>Databases</primary>
          <secondary>backup and restore</secondary>
        </indexterm>Backup</title>
      <para>Firebird comes with two utilities for backing up and restoring your databases:
      <firstterm>gbak</firstterm> and <firstterm>nbackup</firstterm>. Both can be found in the
      <filename class="directory">bin</filename> subdirectory of your Firebird installation.
      Firebird databases can be backed up while users are connected to the system and going about
      their normal work. The backup will be taken from a snapshot of the database at the time the
      backup began.</para>
      <para>Regular backups and occasional restores should be a scheduled part of your database
      management activity.</para>
      <warning>
        <para>Except in nbackup's lock mode, do not use external proprietary backup utilities or
        file-copying tools such as <application>WinZip</application>,
        <application>tar</application>, <application>copy</application>,
        <application>xcopy</application>, etc., on a database which is running. Not only will the
        backup be unreliable, but the disk-level blocking used by these tools can corrupt a running
        database.</para>
      </warning>
      <important>
        <para>Study the warnings in the next section about database activity during restores!</para>
      </important>
      <para>More information about <application>gbak</application> can be found here (HTML and PDF
      version, same content):</para>
      <blockquote>
        <para><ulink
        url="http://www.firebirdsql.org/manual/gbak.html">http://www.firebirdsql.org/manual/gbak.html</ulink></para>
        <para><ulink
        url="http://www.firebirdsql.org/pdfmanual/Firebird-gbak.pdf">http://www.firebirdsql.org/pdfmanual/Firebird-gbak.pdf</ulink></para>
        <!--Both links still valid on new site.-->
      </blockquote>
      <para>The <application>nbackup</application> manual is here (again same content in HTML and
      PDF):</para>
      <blockquote>
        <para><ulink
        url="http://www.firebirdsql.org/manual/nbackup.html">http://www.firebirdsql.org/manual/nbackup.html</ulink></para>
        <para><ulink
        url="http://www.firebirdsql.org/pdfmanual/Firebird-nbackup.pdf">http://www.firebirdsql.org/pdfmanual/Firebird-nbackup.pdf</ulink></para>
        <!--Both links still valid on new site.-->
      </blockquote>
    </section>
    <section id="qsg25-safety-howtocorrupt">
      <title><indexterm>
          <primary>Databases</primary>
          <secondary>corruption</secondary>
        </indexterm>How to corrupt a database</title>
      <para>The following sections constitute a summary of things <emphasis>not</emphasis> to do if
      you want to keep your Firebird databases in good health.</para>
      <section id="qsg25-safety-howtocorrupt-metadata">
        <title><indexterm>
            <primary>Databases</primary>
            <secondary>system tables</secondary>
          </indexterm><indexterm>
            <primary>Databases</primary>
            <secondary>metadata</secondary>
          </indexterm><indexterm>
            <primary>System tables</primary>
          </indexterm>Modifying metadata tables yourself</title>
        <para>Firebird stores and maintains all of the metadata for its own and your user-defined
        objects in special tables, called <firstterm>system tables</firstterm>, right in the
        database itself. The identifiers for these system tables, their columns and several other
        types of system objects begin with the characters <literal>RDB$</literal>.</para>
        <para>Because these are ordinary database objects, they can be queried and manipulated just
        like your user-defined objects. However, just because you can does not mean you should. The
        Firebird engine implements a high-level subset of SQL (DDL) for the purpose of defining and
        operating on metadata objects, typically through <database>CREATE</database>,
        <database>ALTER</database> and <database>DROP</database> statements.</para>
        <para>It cannot be recommended too strongly that you use DDL – not direct SQL operations on
        the system tables – whenever you need to alter or remove metadata. Defer the <quote>hot
        fix</quote> stuff until your skills in SQL and your knowledge of the Firebird engine become
        very advanced. A wrecked database is neither pretty to behold nor cheap to repair.</para>
      </section>
      <section id="qsg25-safety-howtocorrupt-fw">
        <title><indexterm>
            <primary>Forced writes</primary>
          </indexterm>Disabling forced writes</title>
        <para>Firebird is installed with forced writes (synchronous writes) enabled by default.
        Mutations are written to disk immediately upon posting.</para>
        <para>It is possible to configure a database to use asynchronous data writes – whereby
        modified or new data are held in the memory cache for periodic flushing to disk by the
        operating system's I/O subsystem. The common term for this configuration is
        <firstterm>forced writes off</firstterm> (or <firstterm>disabled</firstterm>). It is
        sometimes resorted to in order to improve performance during large batch operations.</para>
        <section id="qsg25-safety-howtocorrupt-fw-win">
          <title>Disabling forced writes on Windows</title>
          <para>The big warning here is: do <emphasis>not</emphasis> disable forced writes on a
          Windows server. It has been observed that the Windows server platforms do not flush the
          write cache until the Firebird service is shut down. Apart from power interruptions, there
          is just too much that can go wrong on a Windows server. If it should hang, the I/O system
          goes out of reach and your users' work will be lost in the process of rebooting.</para>
          <note>
            <para>Windows 9x and ME do not support deferred data writes.</para>
          </note>
        </section>
        <section id="qsg25-safety-howtocorrupt-fw-linux">
          <title>Disabling forced writes on Linux</title>
          <para>Linux servers are safer for running an operation with forced writes disabled
          temporarily. Still, do not leave it disabled once your large batch task is completed,
          unless you have a very robust fall-back power system.</para>
          <!--Linux forced write warning removed, since this issue was fixed in 2.1 and backported to 2.0.4.-->
          <!--However, the security database was created with FW off (until when?). Inform readers that when upgrading an old secbase, they have
to turn FW on themselves. This must be in the general forced writes section (not Linux-specific).-->
          <indexterm>
            <primary>Forced writes</primary>
            <secondary>Linux forced writes bug</secondary>
          </indexterm>
        </section>
      </section>
      <section id="qsg25-safety-howtocorrupt-restore-running">
        <title><indexterm>
            <primary>Restore</primary>
            <secondary>to a running database</secondary>
          </indexterm><indexterm>
            <primary>Databases</primary>
            <secondary>backup and restore</secondary>
          </indexterm>Restoring a backup to a running database</title>
        <para>One of the restore options in the <application>gbak</application> utility
        (<literal>gbak</literal> <literal>-rep[lace_database]</literal>) allows you to restore a
        gbak file over the top of an existing database. It is possible for this style of restore to
        proceed without warning while users are logged in to the database. Database corruption is
        almost certain to be the result.</para>
        <note>
          <para>Notice that the shortest form of this command is <literal>gbak</literal>
          <literal>-rep</literal>, not <literal>gbak</literal> <literal>-r</literal> as it used to
          be in previous Firebird versions. What happened to <literal>gbak</literal>
          <literal>-r</literal>? It is now short for <literal>gbak</literal>
          <literal>-recreate_database</literal>, which functions the same as <literal>gbak</literal>
          <literal>-c[reate]</literal> and throws an error if the specified database already exists.
          You can force overwriting of the existing database by adding the
          <literal>o[verwrite]</literal> flag though. This flag is only supported with
          <literal>gbak</literal> <literal>-r</literal>, not with <literal>gbak</literal>
          <literal>-c</literal>.</para>
          <para>These changes have been made because many users thought that the
          <literal>-r</literal> switch meant <emphasis>restore</emphasis> instead of replace – and
          only found out otherwise when it was too late.</para>
        </note>
        <warning>
          <para>Be aware that you will need to design your admin tools and procedures to prevent any
          possibility for any user (including <database>SYSDBA</database>) to restore to your active
          database if any users are logged in.</para>
        </warning>
        <para>If is practicable to do so, it is recommended to restore to spare disk space using the
        <literal>gbak</literal> <literal>-c[reate]</literal> option and test the restored database
        using <application>isql</application> or your preferred admin tool. If the restored database
        is good, shut down the old database (you can use the <application>gfix</application>
        command-line tool for this; see <ulink
        url="http://www.firebirdsql.org/manual/gfix.html">http://www.firebirdsql.org/manual/gfix.html</ulink>
        or <ulink
        url="http://www.firebirdsql.org/pdfmanual/Firebird-gfix.pdf">http://www.firebirdsql.org/pdfmanual/Firebird-gfix.pdf</ulink>).
        Make a filesystem copy of the old database just in case and then copy the restored database
        file(s) over their existing counterparts.</para>
        <!--Both links to gfix docs still valid on new site.-->
      </section>
      <section id="qsg25-safety-howtocorrupt-restore-login">
        <title><indexterm>
            <primary>Restore</primary>
            <secondary>user logins during restore</secondary>
          </indexterm><indexterm>
            <primary>Databases</primary>
            <secondary>backup and restore</secondary>
          </indexterm>Allowing users to log in during a restore</title>
        <para>If you do not block access to users while performing a restore using
        <literal>gbak</literal> <literal>-rep[lace_database]</literal> then users may be able to log
        in and attempt to do operations on data. Corrupted structures will result.</para>
      </section>
    </section>
  </section>
  <section id="qsg25-gettinghelp">
    <title><indexterm>
        <primary>Help</primary>
      </indexterm><indexterm>
        <primary>Documentation</primary>
      </indexterm>How to get help</title>
    <para>The community of willing helpers around Firebird goes a long way back, to many years
    before the source code for its ancestor, InterBase® 6, was made open source. Collectively, the
    Firebird community does have all the answers! It even includes some people who have been
    involved with it since it was a design on a drawing board in a bathroom in Boston.</para>
    <itemizedlist>
      <listitem>
        <para>Visit the official Firebird Project site at <ulink
        url="http://www.firebirdsql.org">http://www.firebirdsql.org</ulink> and join the user
        support lists, in particular <systemitem class="newsgroup">firebird-support</systemitem>.
        Look at <ulink
        url="http://www.firebirdsql.org/en/mailing-lists/">http://www.firebirdsql.org/en/mailing-lists/</ulink>
        for instructions.</para>
      </listitem>
      <listitem>
        <para>Use the Firebird documentation index at <ulink
        url="http://www.firebirdsql.org/en/documentation/">http://www.firebirdsql.org/en/documentation/</ulink>.</para>
      </listitem>
      <listitem>
        <para>Visit the Firebird knowledge site at <ulink
        url="http://www.ibphoenix.com">http://www.ibphoenix.com</ulink> to look up a vast collection
        of information about developing with and using Firebird. IBPhoenix also sells a Developer CD
        with the Firebird binaries and lots of documentation.</para>
      </listitem>
      <listitem>
        <para><indexterm>
            <primary>Books</primary>
            <secondary>The Firebird Book</secondary>
          </indexterm><indexterm>
            <primary>Firebird Book</primary>
          </indexterm>Order the official Firebird Book at <ulink
        url="http://www.ibphoenix.com/products/books/firebird_book">http://www.ibphoenix.com/products/books/firebird_book</ulink>,
        for more than 1100 pages jam-packed with Firebird information. <emphasis>[Notice: Currently
        out of print; Second Edition in the making. First Edition still available through e.g. eBay,
        Amazon.]</emphasis></para>
      </listitem>
      <listitem>
        <para>As a last resort – since our documentation is still incomplete – you can consult the
        InterBase 6.0 beta manuals (the files whose names start with <literal>60</literal> at <ulink
        url="http://www.ibphoenix.com/downloads/">http://www.ibphoenix.com/downloads/</ulink>) in
        combination with the Firebird 1.5 and 2.<replaceable>x</replaceable> Release Notes.</para>
      </listitem>
    </itemizedlist>
  </section>
  <section id="qsg25-givinghelp">
    <title><indexterm>
        <primary>Help</primary>
      </indexterm><indexterm>
        <primary>Support Firebird</primary>
      </indexterm><indexterm>
        <primary>Firebird Foundation</primary>
      </indexterm>How to give help</title>
    <para>Firebird exists, and continues to be improved, thanks to a community of volunteers who
    donate their time and skills to bring you this wonderful piece of software. But volunteer work
    alone is not enough to keep an enterprise-level RDBMS such as Firebird up-to-date. The <ulink
    url="http://www.firebirdsql.org/en/firebird-foundation/"><emphasis>Firebird
    Foundation</emphasis></ulink> supports Firebird development financially by issuing grants to
    designers and developers. If Firebird is useful to you and you'd like to give something back,
    please visit the Foundation's pages and consider making a donation or becoming a member or
    sponsor.</para>
  </section>
  <section id="qsg25-firebird-project">
    <title><indexterm scope="local">
        <primary>Project</primary>
      </indexterm><indexterm>
        <primary>Firebird project</primary>
      </indexterm>The Firebird Project</title>
    <para>The developers, designers and testers who gave you Firebird and several of the drivers are
    members of the Firebird open source project at SourceForge, that amazing virtual community that
    is home to thousands of open source software teams. The Firebird project's address there is
    <ulink
    url="http://sourceforge.net/projects/firebird/">http://sourceforge.net/projects/firebird/</ulink>.
    At that site are the source code tree, the download packages and a number of technical files
    related to the development and testing of the codebases.</para>
    <para>The Firebird Project developers and testers use an email list forum – <systemitem
    class="newsgroup">firebird-devel@lists.sourceforge.net</systemitem> – as their <quote>virtual
    laboratory</quote> for communicating with one another about their work on enhancements,
    bug-fixing and producing new versions of Firebird.</para>
    <para>Anyone who is interested in watching their progress can join this forum. However, user
    support questions are a distraction which they do not welcome. Please do not try to post your
    user support questions there! These belong in the <systemitem
    class="newsgroup">firebird-support</systemitem> group.</para>
    <para>Happy Firebirding!</para>
  </section>
  <appendix id="qsg25-appx-architectures">
    <title><indexterm class="singular" scope="local">
        <primary>Classic Server</primary>
      </indexterm><indexterm class="singular" scope="local">
        <primary>Superserver</primary>
      </indexterm><indexterm class="singular" scope="local">
        <primary>SuperClassic</primary>
      </indexterm><indexterm>
        <primary>Architectures</primary>
      </indexterm><indexterm>
        <primary>Server architectures</primary>
      </indexterm>Firebird server architectures</title>
    <para>This table gives a more detailed overview of the Firebird server architectures that were
    introduced in the section <link linkend="qsg25-classic-or-super"><citetitle>Classic,
    SuperClassic or Superserver</citetitle></link> near the beginning of this guide.</para>
    <!--See also http://www.iblogmanager.com/download/misc/articles/fb25_architecture_comparison.pdf-->
    <table id="qsg25-tbl-architectures">
      <?dbfo keep-together='auto'?>
      <title>Classic, SuperClassic and Superserver characteristics</title>
      <tgroup cols="4">
        <colspec colname="col-feature" colwidth="*" />
        <colspec colname="col-classic" colwidth="2*" />
        <colspec colname="col-superclassic" colwidth="2*" />
        <colspec colname="col-super" colwidth="2*" />
        <thead>
          <row>
            <entry></entry>
            <entry align="center"><para>Classic Server</para></entry>
            <entry align="center"><para>SuperClassic</para></entry>
            <entry align="center"><para>Superserver</para></entry>
          </row>
        </thead>
        <tbody>
          <row valign="top">
            <entry><para><emphasis>Processes</emphasis></para></entry>
            <entry><para>A listener process (<systemitem class="process">xinetd</systemitem> on
            Linux, <systemitem class="process">fb_inet_server</systemitem> on Windows) listens to
            the Firebird port and spawns a separate server process (<systemitem
            class="process">fb_inet_server</systemitem>) for every client connection.</para></entry>
            <entry><para>A single process (<systemitem class="process">fb_smp_server</systemitem>)
            listens to the Firebird port and serves all connections, using threads to handle
            requests.</para></entry>
            <entry><para>A single process (<systemitem class="process">fbserver</systemitem>)
            listens to the Firebird port and serves all connections, using threads to handle
            requests.</para></entry>
          </row>
          <row valign="top">
            <entry><para><emphasis>Cache</emphasis></para></entry>
            <entry><para>Each server process (= each connection) has its own cache.</para></entry>
            <entry><para>Separate cache spaces for each connection.</para></entry>
            <entry><para>A shared cache space for all connections.</para></entry>
          </row>
          <row valign="top">
            <entry><para><emphasis>Local connections (Linux)</emphasis></para></entry>
            <entry nameend="col-superclassic" namest="col-classic"><para>Applications can use the
            <filename class="libraryfile">libfbembed</filename> library for local connections. This
            is a client and server rolled into one, permitting fast, direct I/O to database files.
            The user process must have filesystem-level access rights to the database for this to
            work. Local connections via the network layer are also possible; in this case, only the
            server process needs access to the database file.</para></entry>
            <entry><para>All local connections are made via the network layer, using <systemitem
            class="systemname">localhost</systemitem> (often implicitly). Only the server process
            needs access to the database file.</para></entry>
          </row>
          <row valign="top">
            <entry><para><emphasis>Local connections (Windows)</emphasis></para></entry>
            <entry nameend="col-super" namest="col-classic"><para>On Windows, all variants support
            safe and reliable local connections using the <systemitem
            class="protocol">XNET</systemitem> protocol, with only the server process requiring
            access rights to the database file. Direct access in user process space requires a
            separate package, <link linkend="qsg25-classic-or-super-embedded">Windows Embedded
            Server</link>.</para></entry>
          </row>
          <row valign="top">
            <entry><para><emphasis>Simultaneous access by multiple engines</emphasis></para></entry>
            <entry nameend="col-superclassic" namest="col-classic"><para>Classic and SuperClassic
            use a shared lock system. This allows access by multiple servers (e.g. a regular server
            and one or more embedded servers) at the same time.</para></entry>
            <entry><para>Superserver acquires an exclusive lock on the database file. No other
            processes can access the database during the same time.</para></entry>
          </row>
          <row valign="top">
            <entry><para><emphasis>Multiprocessor / multicore</emphasis></para></entry>
            <entry nameend="col-superclassic" namest="col-classic"><para>SMP (symmetrical
            multi-processing) is supported out of the box. The
            <parameter>CpuAffinityMask</parameter> parameter in <filename>firebird.conf</filename>
            is ignored.</para><para>Unlike Superserver, Classic and SuperClassic can also assign
            multiple connections to the same database to different processors.</para></entry>
            <entry><para>Windows Superserver defaults to using the first logical processor only,
            because prior to 2.5 it performed badly on SMP systems. To make use of all your
            processors, set the <parameter>CpuAffinityMask</parameter> parameter in
            <filename>firebird.conf</filename> to: 3 for 2 CPUs/cores; 15 for 4 CPUs/cores; 255 for
            8 CPUs/cores.</para><para>Linux Superserver ignores
            <parameter>CpuAffinityMask</parameter>.</para></entry>
          </row>
          <row valign="top">
            <entry><para><emphasis>Guardian (Linux)</emphasis></para></entry>
            <entry><para>The Guardian isn't used: <systemitem class="process">xinetd</systemitem>
            listens for incoming connections and creates server instances as needed.</para></entry>
            <entry nameend="col-super" namest="col-superclassic"><para>SuperClassic and Superserver
            are single-process servers, which can be watched over by the Guardian.</para></entry>
          </row>
          <row valign="top">
            <entry><para><emphasis>Guardian (Windows)</emphasis></para></entry>
            <entry nameend="col-superclassic" namest="col-classic"><!--REMOVE ROW WHEN THIS IS FIXED--><para>When
            run as a Windows <emphasis>application</emphasis> (as opposed to a service), you can't
            use the Firebird Guardian. Note that running Firebird as an application is the only
            option on Windows 9x–ME.<!--Does Fb 2.5 work on these?--> The Windows installer doesn't
            offer the Guardian option at all for Classic/SuperClassic.</para></entry>
            <entry><para>Can be used with the Guardian on Windows, whether run as an application or
            as a service.</para></entry>
          </row>
          <row valign="top">
            <entry><para><emphasis>Events</emphasis></para></entry>
            <entry nameend="col-super" namest="col-classic"><para>As of version 2.5, all models can
            use Fiebird events under all circumstances. If the server is behind a firewall or if
            connections are made through a secure tunnel, a specific events port has to be assigned
            to the <varname>RemoteAuxPort</varname> variable in <filename>firebird.conf</filename>,
            and the firewall or tunnel configured accordingly.</para></entry>
          </row>
        </tbody>
      </tgroup>
    </table>
    <para>The differences listed above result in the following pros and cons of the various
    models:</para>
    <itemizedlist>
      <listitem>
        <para>If a traditional Classic server process crashes, the other connections remain
        unaffected. If a SuperClassic or Superserver process crashes, all the connections go down
        with it.</para>
      </listitem>
      <listitem>
        <para>The Guardian gives some extra reliabilty because it automatically restarts a crashed
        server. On Windows, this gives Superserver a little edge, especially if you run Firebird as
        an application (crashed services can also be restarted by the OS).</para>
      </listitem>
      <listitem>
        <para>Traditional Classic uses less resources if the number of connections is low.
        SuperClassic and Superserver are more efficient if the number of simultaneous connections
        grows.</para>
      </listitem>
      <listitem>
        <para>The local access mode provided by Classic and SuperClassic on Linux, though very fast,
        doesn't offer full security. You can disable it (see the <link
        linkend="qsg25-config-security">Security</link> chapter), but then you also lose the speed
        benefit.</para>
      </listitem>
      <listitem>
        <para>While working with a Windows Embedded application, it may be useful if you can open
        its database through the regular server at the same time, e.g. for <quote>live</quote>
        inspection or to make a backup. This is only possible if your regular server is a Classic or
        SuperClassic.</para>
      </listitem>
    </itemizedlist>
    <para>As you can see, none of the three models is better in all respects. If, after studying the
    above information, you're still not sure which is best for you, 64-bits SuperClassic may be a
    good choice—unless you need the additional connection-level robustness of traditional Classic,
    where a crashing server doesn't take down the other connections. On 32-bits systems,
    SuperClassic may run out of memory under high load; in this scenario, Superserver and
    (especiallly) Classic Server are preferred.</para>
    <para>As mentioned earlier in this guide, you can always switch models later. De-installing and
    installing another server is usually a matter of minutes, and your applications and databases
    will keep functioning like before. The differences are in the servers only, not in the
    databases. All Firebird databases have the same architecture and can be accessed by any type of
    Firebird server.</para>
  </appendix>
  <appendix id="qsg25-dochist">
    <title><indexterm>
        <primary>Document history</primary>
      </indexterm>Document History</title>
    <para>The exact file history is recorded in the <filename class="directory">manual</filename>
    module in our CVS tree; see <ulink
    url="http://firebird.cvs.sourceforge.net/viewvc/firebird/manual/src/docs/firebirddocs/quickstartguide-2.5.xml?view=log">http://firebird.cvs.sourceforge.net/viewvc/firebird/manual/src/docs/firebirddocs/quickstartguide-2.5.xml?view=log</ulink></para>
    <para><revhistory>
        <revision>
          <revnumber>0.0</revnumber>
          <date>2002</date>
          <authorinitials>IBP</authorinitials>
          <revdescription>
            <para>Published as Chapter One of <citetitle>Using Firebird</citetitle>.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>1.0</revnumber>
          <date>2003</date>
          <authorinitials>IBP</authorinitials>
          <revdescription>
            <para>Published separately as a free Quick Start Guide.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>1.x</revnumber>
          <date>June 2004</date>
          <authorinitials>IBP</authorinitials>
          <revdescription>
            <para>Donated to Firebird Project by IBPhoenix.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>2.0</revnumber>
          <date>27 Aug 2004</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para>Upgraded to Firebird 1.5</para>
            <para>Added Classic vs. Superserver section.</para>
            <para>Reorganised and corrected Disk Locations Table.</para>
            <para>Added (new) screenshots.</para>
            <para>Added section on security.</para>
            <para>Updated and completed information on Control Panel applets.</para>
            <para>Added more examples to <quote>Expressions involving
            <constant>NULL</constant></quote>.</para>
            <para>Various other corrections and additions.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>2.1</revnumber>
          <date>20 Feb 2005</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para>Enhanced GSEC section.</para>
            <para>Added more info to <database>CONNECT</database> and <database>CREATE
            DATABASE</database> sections.</para>
            <para>Added version number and document history.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>2.1.1</revnumber>
          <date>1 Mar 2005</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para>Changed <literal>gbak r[estore]</literal> to <literal>r[eplace]</literal> in two
            places.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>2.1.2</revnumber>
          <date>8 Apr 2005</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para>Reordered Firebird SQL subsections.</para>
            <para>Added links to Firebird Null Guide.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>2.2</revnumber>
          <date>2 Dec 2005</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para>Removed "Using the books by IBPhoenix" as it doesn't make sense in the QSG.</para>
            <para>Promoted "How to get help" to 1st-level section and removed "Where to next"
            shell.</para>
            <para>Removed link to UFB and RefGuide; added a note instead explaining their current
            status.</para>
            <para>Updated/corrected classic-super comparison table.</para>
            <para>Moved a number of sections on installing, working with databases, and (un)safety
            into newly created top-level sections.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>2.2.1</revnumber>
          <date>22 Dec 2005</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para>Corrected statement on SS thread usage in Classic-vs-Superserver table.</para>
            <para>Fixed broken link.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>3.0</revnumber>
          <date>21 May 2006</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para>Creation of 2.0 Quick Start Guide, still equal to previous revision except for
            some version numbers, XML ids etc.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>3.2</revnumber>
          <date>10 Aug 2006</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para>Promoted <quote>Firebird Project members</quote> to co-authors in
            articleinfo.</para>
            <para>Updated references to website (<literal>firebird.sourceforge.net</literal> -&gt;
            <literal>www.firebirdsql.org</literal>).</para>
            <para>Removed <quote>maturity</quote> and <quote>Service Manager</quote> rows from
            Classic-vs-Super table; these things are no longer different in Firebird 2. Also changed
            the row on local connections: CS and SS now both allow safe, reliable local connections
            on Windows. Added row on Guardian. Prepended a column with feature names.</para>
            <para>Removed any and all remarks about Classic not having a (full) Service
            Manager.</para>
            <para>Removed 2nd paragraph of <quote>Default disk locations</quote> section.</para>
            <para>Removed notes stating that Classic/Win connections will fail without a host
            name.</para>
            <para>Updated location table and inserted rows for documentation.</para>
            <para>Edited the Installation sections; added sections on Guardian and installing
            multiple servers. Removed <quote>if-you-do-not-find-the-release-notes</quote>
            tip.</para>
            <para>Heavily edited and extended the <quote>Testing your installation</quote>
            sections.</para>
            <para>The <quote>Other things you need</quote> section is now gone and its contents
            distributed across other sections.</para>
            <para>Added a section on gsec (consisting partly of existing material).</para>
            <para>Greatly enhanced and extended the <citetitle>Security</citetitle> section, and
            moved it to another location.</para>
            <para>Extended and improved the <quote>Windows Control Panel applets</quote>
            section.</para>
            <para>Edited <quote>Working with databases</quote>. Added a special section on
            connection strings. Added information on access to database objects, the
            <literal>EXIT</literal> statement, and local vs. remote connections. Made some paths in
            the examples relative, to keep the lines short. Extended paragraph on metadata.</para>
            <para>Weakened the claim that Firebird is more SQL-compliant than any other
            RDBMS.</para>
            <para>Changed the <quote>Expressions involving <constant>NULL</constant></quote>
            section. Added a subsection on <database>DISTINCT</database>. Changed <quote>More about
            <constant>NULL</constant>s</quote> subsection somewhat.</para>
            <para>Renamed <quote>Safety measures</quote> to <quote>Preventing data loss</quote>. The
            Security subsection has been moved elsewhere.</para>
            <para>Extended <citetitle>Backup</citetitle> section to include nbackup information.
            Added links to other documentation.</para>
            <para>In the <quote>How to corrupt...</quote> part, changed gbak -r syntax to -rep and
            added explanatory note.</para>
            <para>Added the <quote>IB6 plus rlsnotes</quote> as last-resort option to <citetitle>How
            to get help</citetitle>. Also mentioned firebird-support explicitly.</para>
            <para>Corrected more version numbers, paths, and stuff.</para>
            <para>Many sections have been reshuffled, moved up or down the hierarchy, etc. Many
            smaller modifications are not listed here.</para>
            <para>Added <quote>Happy Firebirding!</quote> to conclude the last section.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>3.3</revnumber>
          <date>15 Oct 2006</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para>Default disk locations table: added isql to command line tools; added row for
            additional server-side libs.</para>
            <para>Added introductory paragraph to <quote>Installing Firebird</quote>. Changed first
            sentence of <quote>Installing on Linux...</quote></para>
            <para>Changed and extended <quote>Server check: Linux and other Unices</quote>.</para>
            <para>Corrected and extended the section on Linux client-only installs.</para>
            <para>Security section: moved last paragraph of the <quote>Protect databases...</quote>
            listitem into a new item on Classic local mode.</para>
            <para>Connection strings: improved and extended introductory paragraph; added a
            subsection on third party program requirements.</para>
            <para>Changed 3rd and 4th paragraph of <quote>Connecting to an existing
            database</quote>. Used relative paths in connection examples. Updated/corrected note on
            the use of quote characters.</para>
            <para>Edited first <quote>Important</quote> item in <quote>The CREATE DATABASE
            statement</quote>.</para>
            <para>Updated the warning about concatenation of long strings.</para>
            <para>Extended the note in <quote>Restoring a backup to a running
            database</quote>.</para>
            <para>Updated last sentence of first paragraph in <quote>The Firebird
            Project</quote>.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>3.4</revnumber>
          <date>25 Jan 2007</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para><citetitle>About this guide</citetitle>: Changed note about versions and replaced
            HTML and PDF links with single link to new doc index page.</para>
            <para><citetitle>Classic or Superserver?</citetitle>: Replaced note on Embedded Server
            with a proper subsection, containing more info and links to UFB.</para>
            <para><citetitle>Default disk locations</citetitle>: Created two subsections (for Linux
            and Windows); also split table in two and removed first column. Introduced placeholders
            <filename class="directory">&lt;ProgramDir&gt;</filename> and <filename
            class="directory">&lt;SystemDir&gt;</filename>. Changed text around tables, changed
            existing note, and added note for Win64 users.</para>
            <para><citetitle>Security</citetitle>: Removed statement that 1.5 Release Notes are
            included with 2.x packages.</para>
            <para><citetitle>More about <constant>NULL</constant>s</citetitle>: Replaced note about
            the Null Guide being updated with a para announcing the availability of the new
            version.</para>
            <para><citetitle>Backup</citetitle>: Updated information on UFB.</para>
            <para><citetitle>How to get help</citetitle>: Updated documentation links and changed
            text here and there.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>3.5</revnumber>
          <date>14 Mar 2007</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para><citetitle>About this guide</citetitle> and <citetitle>Important notice for 64-bit
            Windows users</citetitle>: Minor rewordings.</para>
            <para><citetitle>User management: gsec</citetitle> and <citetitle>Connection
            strings</citetitle>: Added information on enabling local protocol with
            <parameter>IpcName=Global\FIREBIRD</parameter>.</para>
            <para><citetitle>Security :: Use database aliases</citetitle>: Changed type from
            <sgmltag>&lt;database&gt;</sgmltag> to <sgmltag>&lt;literal&gt;</sgmltag> to improve
            output.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>3.6</revnumber>
          <date>21 Sep 2007</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para><citetitle>About this guide</citetitle>: Mentioned 2.0.3. Warned against
            2.0.2.</para>
            <para><citetitle>Expressions involving <constant>NULL</constant></citetitle>: Space
            added to expected concatenation result: <quote><literal>Home sweet
            </literal></quote>.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>3.7</revnumber>
          <date>8 Apr 2008</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para><citetitle>About this guide</citetitle>: Added 2.0.4 and 2.1 to covered versions.
            Mentioned forced writes bug.</para>
            <para><citetitle>Installing the Firebird server :: Use the Guardian?</citetitle>: Added
            warning about Win installer not detecting existing server.</para>
            <para><citetitle>How to corrupt a database?</citetitle>: Gave subsections <sgmltag
            class="attribute">id</sgmltag> attributes.</para>
            <para><citetitle>Disabling forced writes on Windows</citetitle>: Created new parent
            section <citetitle>Disabling forced writes</citetitle>, with the Windows and Linux cases
            as subsections. Warned against Linux forced writes bug.</para>
            <para><citetitle>License notice</citetitle>: Copyright end year now 2008.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>3.8</revnumber>
          <date>18 Jan 2009</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para><citetitle>About this guide</citetitle>: Added 2.0.5 and 2.1.2 to covered
            versions.</para>
            <para><citetitle>Preventing data loss :: Backup</citetitle>: Mentioned nbackup's
            brokenness in 2.1.</para>
            <para><citetitle>How to corrupt a database :: Restoring a backup to a running
            database</citetitle>: Changed and extended instructions re. safe restoring.</para>
            <para><citetitle>How to give help</citetitle>: New section directing reader to the
            Foundation.</para>
            <para><citetitle>License notice</citetitle>: Copyright end year now 2009.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>3.9</revnumber>
          <date>4 Sep 2010</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para><citetitle>About this guide</citetitle>: Added 2.0.6 and 2.1.3 to covered
            versions.</para>
            <para><citetitle>Working with databases</citetitle> :: <citetitle>Creating a database
            using <application>isql</application></citetitle> :: <citetitle>Starting
            <application>isql</application></citetitle>: Added <quote>↵ means Enter</quote> note,
            like in <citetitle>Connecting with <application>isql</application></citetitle>.</para>
            <para><citetitle>Preventing data loss :: Backup</citetitle>: Mentioned nbackup's
            still-brokenness in 2.1.1 and complete fix in 2.1.2.</para>
            <para><citetitle>How to get help</citetitle>: Last list item: changed version ref.
            <literal>2.0</literal> to <literal>2.<replaceable>x</replaceable></literal>.</para>
            <para><citetitle>License notice</citetitle>: Copyright end year now 2010.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>4.0</revnumber>
          <date>5 Sep 2010</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para>Creation of 2.5 Quick Start Guide, still equal to previous revision except for
            some version numbers, XML ids etc. Also removed erroneous id from primary index term in
            <citetitle>Document History</citetitle> title.<!--Do this in other QSG's too!--></para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>4.1</revnumber>
          <date>15 Sep 2010</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para><citetitle>About this guide</citetitle>: Removed 2.0-specific warnings.</para>
            <para>Upgraded and added information for version 2.5. [Details still to be filled
            in]<!--Do a diff and specify! DON'T FORGET!--></para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>4.2</revnumber>
          <date>21 Sep 2010</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <para><citetitle>Classic, SuperClassic or Superserver</citetitle>: Moved the table into
            an appendix and left only a concise overview here so first-time users can make a
            reasonable choice. Moved the paragraph about installation packages into a separate
            subsection.</para>
            <para><citetitle>User management: gsec :: Trouble running gsec</citetitle>:
            Improved/corrected first listitem.</para>
            <para><citetitle>Firebird server architectures</citetitle>: New appendix, containing the
            (slightly edited) table that used to be in the <citetitle>Classic, SuperClassic or
            Superserver</citetitle> section.</para>
          </revdescription>
        </revision>
        <revision>
          <revnumber>4.3</revnumber>
          <date>8 Jul 2011</date>
          <authorinitials>PV</authorinitials>
          <revdescription>
            <!--EEN AANTAL (ZO NIET ALLE) VAN ONDERSTAANDE WIJZIGINGEN OOK VERWERKEN IN 2.0/2.1 QUICK START GUIDE!-->
            <para>General: Added IDs to all sections that lacked one.</para>
            <para><citetitle>About this guide</citetitle>: Changed ulink to Firebird Doc
            Index.</para>
            <para>New top-level section: <citetitle>The Firebird licenses</citetitle>.</para>
            <para><citetitle>Classic, SuperClassic or Superserver? :: Multiprocessing</citetitle>:
            Changed wording.</para>
            <para><citetitle>Classic, SuperClassic or Superserver?</citetitle>: After differences
            listing, changed advice from flat-out SuperClassic to SuperClassic on 64-bit and
            Superserver/Classic on 32-bit systems under high load. Split paragraph into two.</para>
            <para><citetitle>Classic, SuperClassic or Superserver? :: Installation
            packages</citetitle>: Added instruction on switching to SuperClassic on Linux.</para>
            <para><citetitle>What is in the kit?</citetitle> now comes after the <citetitle>Classic,
            SuperClassic or Superserver</citetitle> section.</para>
            <para><citetitle>Default disk locations :: Linux</citetitle>: Added that default install
            dir may vary per distribution.</para>
            <para><citetitle>Installing Firebird :: Testing your installation :: Note</citetitle>:
            Updated ulink to IB OpGuide (text and url).</para>
            <para><citetitle>Server configuration and management :: User management: gsec ::
            Appointing co-administrators :: Differences between co-administrators and
            <database>SYSDBA</database></citetitle>: Changed wording of 2nd listitem. Completely
            rewrote and extended 3rd listitem. In Note, changed wording of 2nd listitem
            slightly.</para>
            <para><citetitle>Server configuration and management :: Security :: Disable Classic
            local mode on Linux</citetitle>: Took out reference to <filename
            class="libraryfile">libfbembed</filename> library.</para>
            <para><citetitle>Server configuration and management :: Windows Control Panel applets ::
            Firebird Server Manager</citetitle>: Edited last para.</para>
            <para><citetitle>Server configuration and management :: Windows Control Panel applets ::
            Firebird Control Center</citetitle>: Changed almost entire text.</para>
            <para><citetitle>Server configuration and management :: Administration
            tools</citetitle>: Updated ulink text and url.</para>
            <para><citetitle>Preventing data loss</citetitle>: Renamed <citetitle>Protecting your
            data</citetitle>.</para>
            <para><citetitle>Pr<citetitle>otecting your data</citetitle> :: How to corrupt a
            database :: Disabling forced writes on Linux</citetitle>: Removed obsolete indexterm to
            Linux Forced Writes bug.</para>
            <para><citetitle>How to get help</citetitle>: Updated ulink to mailing lists page (text
            and url). Updated ulink to Firebird Doc Index (text and url). Updated link to Firebird
            Book and added notice about first edition being out of print. Removed Note about
            <citetitle>Using Firebird</citetitle> and <citetitle>Firebird Reference
            Guide</citetitle>.</para>
            <para><citetitle>How to give help</citetitle>: Updated Firebird Foundation url.</para>
            <para><citetitle>The Firebird Project</citetitle>: Added terminating slash to ulink
            (text and url).</para>
            <para><citetitle>Firebird Server Architectures</citetitle>: In comparison table, added
            para in Multiprocessor / multicore row for Classic/SuperClassic. Edited final two
            paragraphs.</para>
            <para><citetitle>Document history</citetitle>: Link to CVS changed, points directly to
            document log view in manual module now.</para>
            <para><citetitle>License notice</citetitle>: Copyright end year now 2011.</para>
          </revdescription>
        </revision>
      </revhistory></para>
  </appendix>
  <appendix id="qsg25-license">
    <title><indexterm>
        <primary>License notice</primary>
      </indexterm>License notice</title>
    <para>The contents of this Documentation are subject to the Public Documentation License Version
    1.0 (the <quote>License</quote>); you may only use this Documentation if you comply with the
    terms of this License. Copies of the License are available at <ulink
    url="http://www.firebirdsql.org/pdfmanual/pdl.pdf">http://www.firebirdsql.org/pdfmanual/pdl.pdf</ulink>
    (PDF) and <ulink
    url="http://www.firebirdsql.org/manual/pdl.html">http://www.firebirdsql.org/manual/pdl.html</ulink>
    (HTML).</para>
    <para>The Original Documentation is titled <citetitle>Firebird Quick Start
    Guide</citetitle>.</para>
    <para>The Initial Writer of the Original Documentation is: IBPhoenix Editors.</para>
    <para>Copyright (C) 2002-2004. All Rights Reserved. Initial Writer contact: hborrie at ibphoenix
    dot com.</para>
    <para>Contributor: Paul Vinkenoog - see <link linkend="qsg25-dochist">document
    history</link>.</para>
    <para>Portions created by Paul Vinkenoog are Copyright (C) 2004-2011. All Rights Reserved.
    Contributor contact: paul at vinkenoog dot nl.</para>
  </appendix>
  <index id="qsg25-index">
    <title>Alphabetical index</title>
  </index>
</article>
