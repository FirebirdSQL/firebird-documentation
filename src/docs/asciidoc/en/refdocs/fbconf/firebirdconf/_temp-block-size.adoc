[#fbconf-temo-block-size]
= `TempBlockSize`

Allocation block size for temporary storage.

.Syntax
[listing,subs=+quotes]
----
TempBlockSize = _integer_
----

.Unit
Byte

.Default
`1M` (or `1048576`)

Temporary storage is allocated in increments of `TempBlockSize`.

.Example
[listing]
----
TempBlockSize = 2M
----
