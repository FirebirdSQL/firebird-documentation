[#fbconf-env-isc-password]
= `ISC_PASSWORD`

Default password for a connection.

.Configuration
Client

The value of `ISC_PASSWORD` is used by _fbclient_ as the default value for `isc_dpb_password` and `isc_spb_password` when no explicit password was set.

.See also
<<fbconf-env-isc-user>>
