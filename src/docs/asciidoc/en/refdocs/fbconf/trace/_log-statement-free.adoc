[#fbconf-trace-log-statement-free]
= `log_statement_free`

Log statement free (close cursor, unprepare statement, drop statement handle).
// TODO Double check if all three free types are recorded

.Trace type
`database`

.Syntax
[listing,subs=+quotes]
----
log_statement_free = _Boolean_
----

.Default
`false`

// TODO Add example output
