[#fbconf-trace-log-statement-finish]
= `log_statement_finish`

Log statement execution finish.

.Trace type
`database`

.Syntax
[listing,subs=+quotes]
----
log_statement_finish = _Boolean_
----

.Default
`false`

For statements with a cursor (e.g. `SELECT`), a statement is only finished when the last record has been fetched or the cursor is closed.
// TODO Double check exact behaviour

// TODO Add example output

.See also
<<fbconf-trace-time-threshold>>
