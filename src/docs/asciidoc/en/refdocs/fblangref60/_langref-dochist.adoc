:sectnums!:

[appendix]
[#langref-dochist]
= Document History

The exact file history is recorded in our _git_ repository; see https://github.com/FirebirdSQL/firebird-documentation

[%autowidth, width="100%", cols="4", options="header", frame="none", grid="none", role="revhistory"]
|===
4+|Revision History

|0.1
|TBD
|MR
a|* Copied the _Firebird 5.0 Language Reference_ as a starting point:
** Renamed files and references using _fblangref50_ to _langref_ (new naming convention)
** Where applicable, replaced references to Firebird 5.0 with Firebird 6.0
* Documented `SCHEMA` DDL
* Document `IF EXISTS`/`IF NOT EXISTS` for DDL
* Documented `FORMAT` clause of `CAST`
* Documented `OWNER` clause of `CREATE DATABASE`
* Removed page size 4096
* Removed multi-file database syntax and descriptions
* Documented `CURRENT_SCHEMA` and `SET SEARCH_PATH`
* Documented `RDB$GET_CONTEXT` `SYSTEM` variables `CLIENT_OS_USER`, `CLIENT_VERSION`, `CURRENT_SCHEMA`, `PAGES_ALLOCATED`, `PAGES_FREE`, `PAGES_USED`, and `SEARCH_PATH`.
* Added schema to DDL statement syntax
* Documented `[ACTIVE {vbar} INACTIVE]` for `CREATE INDEX` syntax
* Documented `GREATEST`/`LEAST`
* Removed incorrect mention of decimal point in `FLOAT`
* Documented aggregate functions `ANY_VALUE`, `BIN_AND_AGG`, `BIN_OR_AGG`, `BIT_XOR_AGG`
* Documented system package `RDB$SQL`
* Documented that `DECLARE VARIABLE` can now use any expression as default
* `ACCESS_PATH` in profiler tables/views changed to `BLOB SUB_TYPE TEXT` before Firebird 5.0 release
* Documented `ALTER FUNCTION` of function options
* Documented `ALTER PROCEDURE` and `ALTER PACKAGE` of SQL security options

|===

:sectnums:
